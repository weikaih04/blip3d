# BLIP3D — issue ledger (v12 port), 2026-09-24

One deduplicated list of every misalignment, defect, hidden contract, duplicate, dead path and
naming / path / environment problem found in the five audit reports (`docs/scan/01…05`), each
cross-checked against the old repo (`/fsx/home/weikai.huang/3dgen/model/BLIP3o`, read-only, working tree
at `25c4751` + uncommitted edits). Frame: DESIGN.md §3 (owners), §5 (contracts), §6 (defects), §9 (goldens).
Rows that re-state an item from several reports list every source. "verified" = re-checked in code for
this ledger. **New** = not in any report.

## Summary

**188 items.**

| category | code | count |
|---|---|---|
| defect / train-eval-inference mismatch | a | 38 |
| hidden contract to preserve | b | 76 |
| duplicate implementation to collapse | c | 12 |
| dead code / flag / module | d | 21 |
| naming / path / env / config problem | e | 24 |
| environment / tooling / untracked-file problem | f | 17 |

| disposition | count | meaning |
|---|---|---|
| PRESERVE | 79 | port bit-exactly; BLIP3D enforces it with the listed test |
| FIX | 68 | change in BLIP3D (the concrete fix is in the row) |
| DROP | 24 | not ported |
| COLLAPSE | 13 | one implementation; the reference is named in the row |
| DECIDE | 4 | needs the user (§9 has 19 decisions in total; most others refine a FIX / PRESERVE row) |

| area | rows | a | b | c | d | e | f |
|---|---|---|---|---|---|---|---|
| 1 Conditioning (C) | 46 | 8 | 23 | 5 | 3 | 7 | 0 |
| 2 Training, towers (T) | 32 | 6 | 15 | 1 | 6 | 2 | 2 |
| 3 Unified model + S3 loss (U) | 23 | 9 | 7 | 1 | 4 | 1 | 1 |
| 4 Inference (I) | 27 | 6 | 12 | 4 | 1 | 3 | 1 |
| 5 Data (D) | 28 | 9 | 11 | 1 | 2 | 4 | 1 |
| 6 Checkpoints / config (K) | 9 | 0 | 6 | 0 | 1 | 2 | 0 |
| 7 Environment / repo (E) | 23 | 0 | 2 | 0 | 4 | 5 | 12 |

**Items that change numbers vs v12** (all sit behind `compat_v12`; `compat_v12=True` restores v12):
C-34, C-39 (demo only), T-01, T-02, T-04, T-05, T-06 (the last four only when re-training / resuming),
U-01, U-02, U-03, U-04, U-06, D-03, D-08, plus U-05 and D-07 once decided (`yes*`). Every other row is
numerically neutral (`no`: asserted by a golden) or not numeric (`n.a.`). Four defaults are *kept* at the v12
value but made explicit so they can be A/B'd: C-17, I-07, I-13, I-14.

**DESIGN.md §6 → ledger:** #1 U-01 · #2 T-01 · #3 T-02 · #4 T-04 · #5 U-02 · #6 U-04 + U-05 · #7 U-03 ·
#8 C-24 · #9 C-02, K-04, U-21 · #10 D-07 + D-08 (two issues) · #11 D-11 · #12 T-23 · #13 E-05.

**Legend**
- compat_v12: `yes` = the BLIP3D default differs numerically from v12 and `compat_v12=True` restores v12;
  `yes*` = depends on a decision; `no` = numerically unchanged (a golden proves it); `n.a.` = not numeric.
- Verification: **G1** cond / uncond tensors per tower (train and eval path) · **G2** first-step velocities
  (towers, unified fused) · **G3** final latents per inference mode, fixed seed · **G4** one-batch loss per
  tower and unified + 20-step curve · **G5** sfv50 metrics on 5 cases per mode (all DESIGN §9, recorded in the
  old repo, `compat_v12=True`) · **UT** unit test to write · **RM** re-measurement.
- Old-repo file abbreviations: TN `train_native.py` · TNV `blip3o/model/language_model/trellis_native_vlm.py` ·
  FH `trellis2_blip3o/flow_heads.py` · UG `…/unified_geotex.py` · GS `…/geotex_sampler.py` · LC `…/live_cond.py` ·
  LCB `…/live_cond_batch.py` · EC `…/eval_cond.py` · VC `…/view_code.py` · VCOL `…/vlm_collate.py` ·
  DP `…/demo_pipeline.py` · TH `…/data/tasks/threed.py` · TT `…/data/tasks/threed_textonly.py` ·
  MX `…/data/mixture.py`. `lc/` = `/fsx/data/weikai.huang/lc/`, `_lc/` = `/fsx/home/weikai.huang/_lc/`.
  Report refs: "01 §2.4" = `docs/scan/01_conditioning.md` §2 item 4, etc.
- Owners follow DESIGN §3. Framing (alpha-crop, DINO prep) lives in `cond/prep.py`, not `data/`.
  This overrides 04 §6's `data/views.py` / `data/prompts.py`; `data/views.py` keeps only view picking.

## 1. Conditioning (C)

| ID | item | evidence | disposition | BLIP3D owner | verification | compat_v12 |
|---|---|---|---|---|---|---|
| C-01 | (a) Silent cond-VLM fallback: with `COND_VLM_CKPT` unset every default loads the v2.2 finetune; defaults (`vlm_path=V22_CKPT`) are bound at import time; `TrainCondEncoder()` / `eval_cond.encoder()` do not require the path | 01 §5, §6.2; 02 §0.7; 03 §4.9; 04 §1.2, §7.13; 05 §5a/5b; LC:48-54; LCB:160,244,268 (defaults); eval_suite `run_v12_image.py:33` (comment) | **FIX**: `cond.qwen` = HF id + revision (`89644892…`), required, passed explicitly (no env, nothing bound at import), written to ckpt config, verified on load (mismatch = hard error) | cond/qwen.py, train/ckpt.py | UT `test_encoder_identity` (missing → raise, wrong revision → raise); G1 | n.a. |
| C-02 | (e) `vlm_model="Qwen/Qwen3.5-2B"` in every v12 config.json and launcher; the trainer still loads that AutoProcessor although the live collate branch never uses it (offline start needs it cached) | 01 §5; 02 §2, §7.16; 03 §0.8; 05 §5a; TN:747,1099,1404; `train_native_split.sh:138` (01 says :139); verified VCOL:140-252 never touches `processor` | **DROP** the field and the processor load; identity comes from C-01 | train/trainer.py, train/ckpt.py | UT: offline start without a Qwen3.5 cache | n.a. |
| C-03 | (b) Cond = `hidden_states[-1]` of the full `AutoModelForImageTextToText`, which in transformers 5.2.0 is the post-final-RMSNorm `last_hidden_state` (`tie_last_hidden_states`); version-dependent; the call also computes full-vocab logits (~2.5 GB at bs8×1050) and keeps all 29 hidden states | 01 §2.8, §6.1 ([unverified numerically]); LCB:376-377,531-532; transformers `output_capturing.py:258-268`, `modeling_qwen3_vl.py:936` | **FIX** implementation (call the base model, take `last_hidden_state`, no logits), **PRESERVE** semantics; pin transformers 5.2.0 | cond/qwen.py | G1 + UT one-batch parity vs `TrainCondEncoder.encode` (max-abs 0) | no |
| C-04 | (b) Boilerplate mask = ids of `<\|im_start\|> <\|im_end\|> <\|vision_start\|> <\|vision_end\|> <think> </think>` + "user"(872) + "assistant"(77091), removed anywhere (also a bare "user" inside a caption); `\n` and `[3D Gen]` kept; `keep = am & ~isin(ids, boiler)` | 01 §2.6; LCB:383-384,533; VCOL:84-110 | **PRESERVE** (caption-internal removal is v12 behaviour) | cond/prompts.py | UT `test_boiler_ids` (id list; caption containing "user") | no |
| C-05 | (b) Keep-mask layout differs by mode: I1/IM keep the full sequence (1050 / 1056) + mask; T is compacted to kept tokens; connector attends with `key_mask`; eval compacts the final cond by key mask, training passes the mask to x-attn | 01 §2.7; LCB:549-553; connector.py:195-203; EC:130; FH:1048-1050,1692-1712 | **PRESERVE** both; one assembler returns (tokens, key_mask), the inference wrapper compacts | cond/assemble.py | G1 (train-path cond == eval-path cond after compaction) | no |
| C-06 | (b) Prompts: I1 `"[3D Gen] <\|vision_start\|><\|image_pad\|><\|vision_end\|>\nReconstruct this object in 3D."`; IM = n adjacent image blocks, same text; T `"[3D Gen] " + TXT_PROMPTS[(int(sha[:8],16)+ci) % 3]`, ci indexes the empties-filtered caption list; `apply_chat_template(add_generation_prompt=True)`, no system message (the dead processor branch used False) | 01 §2.1; 04 §2.3; LC:55-58; LCB:72-81,120-122,303-307 | **PRESERVE** verbatim; hash requires a hex sha (synth 20-hex is fine) | cond/prompts.py | UT rendered text + ids for t/i1/im; G1 | no |
| C-07 | (a) Inference text template is inconsistent: the bench hashes the eval case's sha (`prep_t(sha, 0, prompt)`), the demo passes `sha="0"*8` + explicit template 0 | 01 §2.1; `run_unify_bench.py:107`; DP:267 | **FIX**: explicit `template` argument on `encode_text`; bench adapter derives it with the v12 hash, demo default 0; recorded in result json | cond/prompts.py, infer/ | G1 text case; UT | n.a. |
| C-08 | (b) Two caption-tier policies: `text_to_3d_weighted` (real, weight .6) draws over 4 fixed tiers [.4,.12,.08,.4], a missing tier falls back to t000 (long) then `min(ci,len-1)` (3-caption asset → long at 0.8); `text_to_3d` (synth, .1) renormalises [.45,.15,.15,.25] over existing captions | 01 §2.4, §4.12; 04 §2.3, §7.11; TT:56-62,91,99-104; TH:238-252 | **PRESERVE** both through one sampler with `fallback: long / renormalize` (see DECIDE-6) | data/task.py | UT `test_caption_sampler` | no |
| C-09 | (e) `text_to_3d_weighted` stamps `_task="text_to_3d"` so the model's string-keyed fuse_dino exemption fires; per-task logs merge real and synth text | 01 §2.5; 04 §2.1, §5.10; TT:105; TNV:1172-1175; TN:702 | **FIX**: route on `modality ∈ {t,i1,im}`; task name only for logging | data/task.py, cond/assemble.py | UT (t batch without DINO passes; image batch without DINO fails) | n.a. |
| C-10 | (e) IM per-view Qwen budget from env `GEOTEX_IM_TOK_PER_VIEW` (default 256; S3 exports it); same-named constant = 128 at LC:65 and an env-read `DINO_SIZE_IM` at LC:64 (dead encoder only); `DINO_IMAGE_SIZE` VCOL:38 (dead) | 01 §2.10, §4.4; 04 §0; 05 §5b; LCB:56,62; `v12_s3.sh:13` | **COLLAPSE** into recipe fields `cond.im_tok_per_view=256`, `cond.dino_size=512`, stored in ckpt; drop the LC copies | cond/prep.py | UT asserts I1 = 1024 image tokens, IM = 256/view | no |
| C-11 | (b) Processor coupling: `Qwen2VLImageProcessorFast` (min_pixels 65536, max_pixels 16777216, patch 16, merge 2), raster order of image_pad tokens; `cap_image` = downscale-only LANCZOS `max(32, round(w*s))`, 1024² → 512² per IM view | 01 §2.10, §6.10; VCOL:52-70 | **PRESERVE**: vendor the processor config, assert token counts | cond/qwen.py, cond/prep.py | UT token-count asserts; G1 | no |
| C-12 | (b) `<\|image_pad\|>` = 151655 resolved by name; pad = 151643; right padding (left padding shifts mRoPE) | 01 §2.9; LCB:392-396,431-442,503-516 | **PRESERVE** | cond/qwen.py | UT | no |
| C-13 | (b) Qwen framing: I1 = full uncropped render `.convert("RGB")` (alpha dropped, not composited); `pixel_values` cast to bf16 on CPU; IM views through `cap_image` | 01 §2.11; 04 §2.2, §7.6 (RGB under alpha=0 is [spec]); LCB:176-186,217,231 | **PRESERVE**; RM: log the RGB under alpha=0 on 100 renders to document it | cond/prep.py | G1; RM | no |
| C-14 | (b) DINO framing order: cap long side 1024 → `_alpha_crop` (α > 0.8·255, fallback > 0; centre (x0+x1)/2; half = max(w,h,2)//2; PIL banker's rounding; out-of-bounds zeros) → `_composite_black` (float RGB·α, uint8 truncation) → MIN_IMG_PX 64 → 512² LANCZOS, /255, fp32; RGB input gets no crop; the yaml `crop_to_object` flag has no effect on the live path | 01 §2.12; 04 §2.2; LCB:134-157,182-183,233-235; TH:346,389-417 | **PRESERVE** bit-exact (pin Pillow 11.3.0); DROP the `crop_to_object` field | cond/prep.py | UT golden image → `dino_px` bytes; G1 | no |
| C-15 | (b) DINO features: ImageNet mean/std on GPU; `DinoV3FeatureExtractor.extract_features` skips the model's final norm and applies unaffine `F.layer_norm` over 1029 tokens (1 CLS + 4 reg + 32×32); `camenduru/dinov3-vitl16-pretrain-lvd1689m` @`3c276edd…`, fp32 | 01 §2.13; LCB:103-104,389-390,541; TRELLIS.2 `image_feature_extractor.py:81-92`; `dino_align.py:34` | **PRESERVE**; model id + revision in ckpt config | cond/dino.py | G1 | no |
| C-16 | (b) Dtype chain: Qwen bf16 → stored fp16 → bf16 (train) / fp32 (eval); DINO fp32 → fp16 → bf16 / fp32; the fp16 intermediate is part of bit parity | 01 §2.14, §6.11; LCB:554,570; TNV:1162-1168 | **PRESERVE** fp16 intermediate (DESIGN §5) | cond/assemble.py | G1 bit-exact | no |
| C-17 | (a) Connector precision train ≠ eval: training runs connectors + stamps in bf16; every eval path casts connectors `.float()` and feeds fp32 cond | 01 §2.14; `eval_fusion_v22.py:55`; `eval_tex_v22.py:57`; `export_glb_fullchain.py:52`; `run_unify_bench.py:75,88-92` (per 01) | **PRESERVE** as explicit preset field `cond_dtype` (v12 eval value fp32); RM bf16 vs fp32 (DECIDE-8) | infer/presets.py | G1 eval path; RM | no (default = v12) |
| C-18 | (b) `qwen_img_rc` = cell centres ((r+.5)/gh, (c+.5)/gw), raster per image restarting at each image, −1 for non-image tokens and padding; I1 32×32 lattice, IM 16×16 (bilinear 2×2 average) | 01 §2.15; LCB:444-476,563-564; TN:447-456 | **PRESERVE** | cond/stamp.py | UT vs old function | no |
| C-19 | (b) `_add_patch_pos`: bilinear, `align_corners=False` (`y=(r·P−0.5).clamp(0,P−1)`), table and weights cast to the cond dtype (bf16 in training), added only where rc ≥ 0 | 01 §2.16; FH:505-546 | **PRESERVE** | cond/stamp.py | UT vs old function | no |
| C-20 | (e) Patch table `sincos2d` (Q=C/4, pos+0.5, × `VIEW_EMBED_SCALE`) reads its scale from env at connector construction; the ckpt buffer wins on strict load | 01 §2.17; connector.py:61-77 | **FIX**: scale is a recipe field stored in ckpt; tables always loaded from ckpt | cond/stamp.py | UT | no |
| C-21 | (b) Segment codes: `Generator(1234)`, a,b = randn, b ⟂ a, both scaled to L2 4.8; row 0 = DINO, row 1 = every Qwen token (image and text); fixed, not learned (comments "zero-init" FH:623,1464-1467 and "[image, text]" FH:553 are wrong) | 01 §2.18; connector.py:168-172 | **PRESERVE** values; fix comments | cond/stamp.py | UT regenerate (bf16) == ckpt | no |
| C-22 | (b) View codes (hashed): row v = unit(randn(C, seed 1_000_003+v, f64)) · 0.2·√(C/2); 16 rows; one model-level buffer shared by all towers and lanes; ids are ordinals within the sample (views sorted by camera id), not camera ids; I1 DINO → row 0, I1 Qwen → none; IM Qwen image tokens → their view's row, IM vision/text tokens → none | 01 §2.19; 02 §4.3; VC:22-29; LCB:478-495,556-557,572-575 | **PRESERVE** | cond/stamp.py | UT; G1 (i1, im-3, im-4) | no |
| C-23 | (b) Every stamp buffer in the ckpts is bf16; regenerated fp32 differs by up to 1.65e-3; DESIGN §7 "dino_view_embed is rebuilt" must mean rebuilt **and bf16-rounded** | 01 §0, §2.20, §6.3; 02 §4.11; verified here: S3 17k holds BF16 `{diffusion,geo,ss}_connector.cond_{seg_embed,patch_pos}` and `dino_view_embed` | **PRESERVE**: load from ckpt; any regeneration rounds through bf16 and is asserted equal | cond/stamp.py, train/ckpt.py | UT over every keep/v12 ckpt | no |
| C-24 | (a) bf16 view table misdetected as "learned" (`allclose` atol 1e-4) → `ensure_view_codes` raises for > 16 views. **New:** even with detection fixed, the hashed extension recovers its scale from the bf16 row-0 norm (rounded to 6 digits), so rows ≥ 16 get a slightly wrong scale | 01 §0, §6.3 (verified by execution); DESIGN #8; VC:40-69; EC:104-112 | **FIX**: store `view_code.mode` and `scale` explicitly; extend with the exact scale, then round to bf16 | cond/stamp.py | UT extend 16 → 24 from a bf16 table == bf16(regenerated) | no (≤ 16 views unchanged) |
| C-25 | (a) `init_from_checkpoint` drops `dino_view_embed` and regenerates it from env `VIEW_EMBED_SCALE` → silent shift if the env differs | 01 §2.19; 02 §6, §7.7; TN:1320-1321 | **FIX**: carry the buffer across init; assert equal to the recipe-derived table | train/ckpt.py | UT | no |
| C-26 | (b) bf16 addition order: Qwen = connector → + view code (IM) → + patch pos → + seg[1]; DINO = to bf16 → + view code → + seg[0] → × keep | 01 §2.21; FH:601-632 and the cascade copy FH:212-242 | **PRESERVE** | cond/assemble.py | G1 bit-exact | no |
| C-27 | (b) CFG uncond is not zero: Qwen half = `connector(0)` + view + patch + seg[1] (T: `connector(0)` + seg[1]); DINO values zeroed after the stamps, keys kept. Comments FH:236-237, 623-625 say otherwise. 02 §4.3 left "inference must match" as [spec] — resolved: `eval_cond.cond_uncond` forces `ext_drops` and reproduces it | 01 §0, §2.22; 02 §4.3, §7.4; 03 §2.2; DESIGN §5; EC:125-139 | **PRESERVE**; fix comments | cond/assemble.py | G1 uncond, train path vs eval path | no |
| C-28 | (b) Dropout semantics, independent per row: `drop` p=0.1 (Qwen hidden zeroed before the connector, DINO values zeroed, all keys stay); `ddrop` p=0.3 (DINO keys masked); `qdrop` (Qwen keys masked, `& ~ddrop`); T rows get only `drop` (`torch.bernoulli`) | 01 §2.22; 02 §4.3; FH:26-35,596-652 | **PRESERVE** | cond/assemble.py | UT draw statistics; G4 | no |
| C-29 | (b) Per-stage dropout: `qwen_drop_prob` = 0.0 in S1 and S2 (the brief said 0.1), 0.1 only in S3; mask 0.1 and DINO 0.3 in every stage | 01 §0, §6.6; 02 §2; TN:898; keep/v12 config.json files | **PRESERVE** as per-stage recipe fields | recipes/, train/stages.py | UT recipe → args parity | no |
| C-30 | (a) Train-mode gating is inconsistent: `dino_drop_prob` is gated by `self.training`, `qwen_drop_prob` and `mask_drop_prob` are not | 01 §2.22; verified TNV:1131,1234-1236,1377-1379 | **FIX**: one `training` flag gates every drop inside the assembler | cond/assemble.py | UT (eval mode → no drops) | no (eval never used this path) |
| C-31 | (b) S3 per-lane drops: the tex lane draws; the SS lane replays tex's realised drops; the geo lane is never dropped (`geotex_joint_cond_drop=False`, verified in the 17k config) | 01 §2.23; 03 §2.2; FH:1066-1128 | **PRESERVE** for S3 compat (defect side: U-05) | losses/unified.py | G4 unified | no |
| C-32 | (b) Eval CFG assembly: cond `ext_drops=(0,dd,qd)`, uncond `(1,dd,qd)`, same modality regime; rows compacted by key mask; `cond_max_length` 10240; view table auto-extended | 01 §2.24; EC:75-140 | **PRESERVE** as the `cond_uncond` API | cond/assemble.py | G1 | no |
| C-33 | (d) `cond_max_length` truncation (Qwen segment only, before the DINO concat) never fires in v12 (largest Qwen segment 1056) | 01 §2.25; 02 §2; FH:175-183,583-588 | **DROP** the truncation; assert length ≤ limit | cond/assemble.py | UT | no |
| C-34 | (c) Two training assemblers: S1/S2 trained through `compute_cascade_flow_loss`'s fusion/plain copy, S3 through `build_unified_cond`; equal on the deterministic path, different RNG draw order | 01 §0, §3, §4.2; 02 §1; `tests/test_unified_conventions.py::test_unified_cond_and_sampler`; FH:203-283 vs 549-679 | **COLLAPSE** into one `assemble(..., drops)`; compat_v12 draws tower-stage drops in the cascade order | cond/assemble.py | G4 per tower with compat on; UT deterministic-path equality | yes (RNG order) |
| C-35 | (c) Encoders: `TrainCondEncoder` (reference, LCB:363-585), `LiveCondEncoder` (LC:280, dead), `TrellisNativeVLM.encode_cond` (dead), offline cache builders | 01 §4.1 | **COLLAPSE** → `CondEncoder` (qwen.py + dino.py); drop the rest | cond/ | G1 | no |
| C-36 | (c) Stale assemblers: `live_cond.build_stage_cond` (no CondStamp — wrong for v12 weights, still used by eval_suite `run_s3_t50.py`), `eval_cond.cond_uncond_from_tensors`, the scripts' `build_cond_*` (cache era) | 01 §3, §4.2; 05 §3c; DP:49-55 | **DROP** | — | CI grep | n.a. |
| C-37 | (c) Small duplicates: `TXT_PROMPTS` (LC:58, LCB:72); `PROMPT_IM` vs `im_prompt`; `_resolve_image_token_id` (LC:302, LCB:431); `_qwen_view_ids` ×3 (LCB:478 reference; LC:340 equal-split; TH:525-531 hardcoded 292-token layout); boiler mask set-loop (LC:326) vs `isin` | 01 §4.3, §4.5, §4.6, §4.9 | **COLLAPSE** to the LCB versions | cond/prompts.py, cond/stamp.py | UT | no |
| C-38 | (c) Framing duplicates: `threed._alpha_crop` (reference) vs `live_cond._square_box` (> 0 mask, `int()` truncation; demo only); DINO prep ×4 (`_dino_image` reference; `_load_views`; `vlm_collate` dino_images with BILINEAR; `DinoV3FeatureExtractor.__call__` list branch) | 01 §4.7, §4.8 | **COLLAPSE**: `_alpha_crop` next to the encoder; `_square_box` stays a documented demo-only rule | cond/prep.py | UT | no |
| C-39 | (a) Demo ≠ training: demo framing is `object_frame` (segmentation, `_square_box`, 0.52 extent, RGB on black), and the demo IM path accepts 1 image (IM with n=1 was never trained; n ∈ {2,3,4}) | 01 §6.8; verified DP:257-258; LCB:244-288 | **FIX**: one image → i1 path, IM requires 2-4; photo framing kept and documented | infer/, cond/prep.py | UT; port `test_demo_v12` | yes (demo output only) |
| C-40 | (b) xf2 connector: in_proj (xavier, zero bias) → 2 pre-norm QK-norm blocks, 8 heads, MLP ×4 tanh-GELU → out LayerNorm; `key_mask` honoured inside | 01 §3; 02 §5; connector.py:56-203 | **PRESERVE**; drop the MLP `TRELLIS2Connector` and the `dino_sig` / `zero` / `*_learn` patch branches | cond/connector.py | G1; UT strict load | no |
| C-41 | (a) Qwen `attn_implementation` is not pinned; batched (train, right-padded) vs bs1 (eval) bf16 noise is acknowledged in code | 01 §6.1; LCB:503-507 | **FIX**: pin to what v12 resolved (record it from the old env, freeze list) and store it | cond/qwen.py | G1 with a documented tolerance | no |
| C-42 | (d) Retired cond features: `pos_stamp.py` (and `DPOS_NPZ`), `dino_sig`, the `sincos` view-code arm, cache builders, `vlm_cache` except `collate_cached`, the raw-processor collate branch, `upscale_image`, `set_default_target_tokens_per_view`, `--target_tokens_per_view 1024` (inert on the live path) | 01 §3; 02 §2, §3; 04 §4; 05 §7 | **DROP** (keep the pad helper) | cond/ | import-closure check | n.a. |
| C-43 | (e) Names: `V22_CKPT`/`_V22_DEFAULT`; `dino_view_embed` (used for both segments) → `view_code`, `dino_view_embed_max` → `max_views`; `diffusion_connector` (= the tower's own in S1/S2, = the tex lane in S3); `xf2`/`TRELLIS2TransformerAdapter`/`cond_adapter`; `live_cond`/`_live_prep`/`LiveCond…` vs `TrainCond…`; `trellis2_blip3o`, `TrellisNativeVLM` | 01 §5; 02 §6; 03 §5 | **FIX** names; old names live only in the ckpt key map (K-01) | cond/, train/ckpt.py | UT key-map round trip | n.a. |
| C-44 | (e) Stale docs: token counts 1054/292/405, DINO@320, `--cond_patch_pos dino_sig` (TN:947, TNV:150), `view_embed_mode` "sincos default" (TN:899), Qwen3.5 comments (connector.py:33, TNV:58). 04 §1.2 repeats the stale "input_ids (1054,)" | 01 §0, §5, §6.12; LCB:12,25,185 | **FIX**: do not port; docs/recipe.md states the measured numbers (1050 / 1056 / 1029) | docs/ | — | n.a. |
| C-45 | (e) `stage_to_shm` hardcodes `/dev/shm/v22ckpt_live` and `condvlm_<sha1>` | 01 §3, §5; LCB:313-359 | **FIX**: optional utility, path from paths.yaml | utils/paths.py, cond/qwen.py | — | n.a. |
| C-46 | (d) DINO parity against the old cond cache (cos 0.9937) is irrelevant: v12 trained live end-to-end | 01 §6.13; LCB:83-95 | **DROP** | — | — | n.a. |

## 2. Training — separate towers, S1 / S2 / SS continuation (T)

| ID | item | evidence | disposition | BLIP3D owner | verification | compat_v12 |
|---|---|---|---|---|---|---|
| T-01 | (a) Timestep t comes from TRELLIS `sample_t` on the **CPU** generator, identical on every rank (HF `set_seed(42)`; `RankRNGCallback` reseeds CUDA only) → 8-16 distinct t per optimizer step instead of 256. Affects S1, S2, SS-cont and the running SS-300k. **S3 is not affected**: `sample_timestep_pairs/triples` and SS_DUAL draw on CUDA (per rank) | 02 §0.1, §4.1, §7.1 (rng_state cpu bit-identical across ranks); DESIGN #2; TRELLIS.2 `trellis2/trainers/flow_matching/flow_matching.py:130-140`; loss.py:177; TN:212-252; verified FH:734-749, 864-868, 1319 (`device=device`) | **DECIDE** (DECIDE-1; rec: per-rank CUDA draw by default, compat reproduces the CPU draw) | losses/flow.py | UT distinct t across 4 simulated ranks; G4 with compat on | yes |
| T-02 | (a) EMA shadow is reset to the raw weights with n=0 on every resume; n is never saved → the final `*_106000_ema` average only the last ~66k (tex) / ~61k (shape) / 31k (SS) steps; the S3 17k EMA covers only the 4-node leg | 02 §0.2, §4.5, §7.2; 03 §2.1; DESIGN #3; TN:77-121; transformers trainer.py:1571/1642 (per 02) | **FIX**: persist shadow + n; restore on resume; `--ema_init_*` becomes derived | train/ema.py, train/ckpt.py | UT save → resume → identical shadow and n; resume golden | yes (re-training only) |
| T-03 | (f) SS-cont launcher's default `--ema_init_from …/s3_ss_v12s2cont/checkpoint-25000` points at a rotated-away dir. **Correction to 02 §0.3**: the live 300k path passes `EMA_INIT`/`EMA_N` explicitly (`ss_to300k.sh:33`, `ss300k_failover.sh:38`), so only a bare `launch_ss_cont.sh` without `EMA_INIT` hits the deleted default | 02 §0.3, §0.5; lc/v12_ss_cont.sh:22 | **FIX** in BLIP3D (EMA restored from the resumed ckpt itself, T-02); old repo: remove the default after the run ends (user's call) | train/ckpt.py, cluster/failover.py | UT resume without an EMA path | n.a. |
| T-04 | (a) Data replay: `mixture_seed=0` + `--ignore_data_skip True`, and the seeds have no step term → every resume / restart (and S2 relative to S1) replays the same index stream. The SS-cont launcher now passes `--mixture_seed ${MIX_SEED:-${EMA_N:-0}}` (the 300k run uses 65000) | 02 §0.5, §2; 04 §0, §5.3, §7.2; DESIGN #4; MX:166-175; lc/v12_ss_cont.sh:22 | **FIX**: resumable sampler (cyclers yield indices without loading, so skipping is cheap); seed = f(run seed, resume step) as fallback; compat = seed 0, no skip | data/mixture.py, train/trainer.py | UT: resume at k yields the index stream of an uninterrupted run | yes |
| T-05 | (a) Weights-only restarts (S2-SS 2n→4n at 75k; S3 5n→4n at 15k) reset optimizer, EMA, LR (100-step re-warm), clip history and data order, and restart local step numbering (`checkpoint-106000` holds `global_step31000`; S3 "17k" = global 32k; SS-cont local = global − 75000) | 02 §2, §4.6, §4.13; 03 §2.1; ss_to300k.sh:3-6 | **FIX**: world-size changes through a universal checkpoint (as `ss300k_failover.sh` already does); all step numbers global (DESIGN §2); how to record history → DECIDE-13 | train/ckpt.py, cluster/failover.py | 200-step resume test from a keep ckpt (loss / grad-norm overlap) | yes (re-training only) |
| T-06 | (a) `AdaptiveGradClipCallback` (p95 of last 1000 norms, max 1.0) buffer is not checkpointed; `BF16_Optimizer` restores `clip_grad`, so the last threshold persists until 1000 new norms; a weights-only restart goes back to flat 1.0; the "no DeepSpeed engine attached" log line is misleading | 02 §4.7; TN:299-319,1497-1507; deepspeed bf16_optimizer.py:479,524 | **FIX**: persist the norm buffer; fix the log | train/clip.py | UT save/restore | yes (resume only) |
| T-07 | (b) Clip mechanics: DS `gradient_clipping=1.0` from `max_grad_norm`; engine bound lazily at the first `training_step`; `assert all_groups_norm > 0` hard-fails on all-zero grads | 02 §4.7; bf16_optimizer.py:309-316 | **PRESERVE** | train/clip.py | UT | no |
| T-08 | (b) Timestep / target dtype: towers cast targets to `cond.dtype` (bf16) so t, t·1000 and x_t are bf16-quantized. **S3 is fp32** (new, verified): `common_dt` = tex target feats, which arrive fp32 because SparseTensors bypass HF's bf16 cast (T-09), so t_s, t_x, t_ss and x_t are fp32. Corrects DESIGN §5 "rounded to bf16" (towers only) and closes 03 §2.3 / §7.9 | 02 §4.1, §7.5; 03 §2.3, §7.9; FH:316,404,452; loss.py:177-181; verified FH:1193-1198,1219-1221 | **PRESERVE** per stage (towers bf16, unified fp32); inference stays fp32 — RM tower inference with bf16-rounded t (DECIDE-9) | losses/flow.py, losses/unified.py | G4; UT dtype asserts | no |
| T-09 | (b) **New hidden contract**: HF `Trainer._prepare_input` casts every dense floating tensor in the batch to the DeepSpeed dtype (bf16): `target_ss_latent` is bf16-rounded in all stages (S3 then upcasts it to fp32), ROAD `align_points` are bf16; SparseTensor targets and `_live_prep` (popped first) are not cast | not in any report; transformers `trainer.py:2213-2220`; TN:603-611 | **PRESERVE** explicitly in collate / loop, so it survives a custom loop (DECIDE-2) | data/collate.py, train/trainer.py | UT batch dtypes; G4 | no |
| T-10 | (b) Loss: plain `F.mse_loss`; dense mean over B×8×16³; sparse mean over all voxels in the batch (large assets weigh more); no per-sample weights; σ_min 1e-5, x_t = (1−t)x0 + (σ+(1−σ)t)ε, v = (1−σ)ε − x0, t_in = 1000t, autocast bf16 | 02 §1, §4.2, §5; loss.py:117-250 | **PRESERVE** | losses/flow.py | G4 | no |
| T-11 | (b) Timestep laws: SS logitNormal(1,1), SLAT uniform | 02 §4.1; DESIGN §5 | **PRESERVE** | losses/flow.py | UT | no |
| T-12 | (b) ROAD on SS: tap `ss_flow.blocks[9]` (`repa_depth 0` → 30//3), AlignMLP 1536→2048→2048→1024 stored as `repa_projector`, Uni3D-g teacher under bf16 autocast, 512 tokens, GPU matcher, 10k points, 0.5·(1−cos global) + 0.1·Hungarian after the gate; CFG- and DINO-dropped rows are aligned too (deliberate) | 02 §1, §4.2, §5, §7.10; TNV:735,749-754; FH:345-384,361-366,492-493; road/loss.py:65-94 | **PRESERVE**; rename `repa_projector` → `road.align` via the key map | models/road/, losses/road.py | ports of test_road_align / test_road_wiring; G4 SS | no |
| T-13 | (b) `_road_step` gate counts **micro**-steps (GA-dependent) and is a persistent buffer carried through `init_from` (402000 in s2_ss 106k) → the token term was on from S2 step 0 | 02 §4.8; TNV:730-731,1027-1028 | **PRESERVE** counter and unit; record GA in ckpt | losses/road.py | UT | no |
| T-14 | (e) Uni3D-g ckpt found by a glob over three HF caches; the GPU matcher JITs into `~/.cache/torch_extensions` | 02 §2; 05 §5a; road/teacher.py:41-44; TN:929 | **FIX**: `paths.uni3d_g` explicit file; `TORCH_EXTENSIONS_DIR` in cluster env | utils/paths.py, cluster/ | fetch_assets hash check | n.a. |
| T-15 | (b) Freezing: S1 `flow_tune none` freezes by name substring; the connector trains only because nothing froze it (the "always trainable" loop is unreachable); S1 SS also trains the ROAD projector; S2 trains everything but `trellis_decoders`; EMA key counts 42/36 (S1) and 682/676 (S2) | 02 §3, §4.4; TN:349-406 | **FIX**: explicit trainable sets; assert the key counts | train/stages.py | UT key counts vs keep/v12 | no |
| T-16 | (b) Fixed buffers are neither trained nor in the EMA: `dino_view_embed`, connector `cond_seg_embed`/`cond_patch_pos`, `ss_flow.rope_phases` (C64), `_road_step` | 02 §4.4; 03 §4.13 | **PRESERVE** | train/ema.py | UT | no |
| T-17 | (b) EMA mechanics: decay 0.9999, warm-up min(d, (1+n)/(10+n)), one update per optimizer step, fp32 shadow sharded by i % world, updated from the **bf16 module weights** (not the fp32 master), gathered over gloo at save, `ema.safetensors` = trainable keys in fp32 | 02 §4.5, §7.9; TN:77-179 | **PRESERVE** (bf16 source kept for parity; DECIDE-15) | train/ema.py | port test_ema_shard (sharded == whole) | no |
| T-18 | (c) `*_ema` product dirs are made by out-of-repo `_lc/mk_ema_init.py` (fp32 EMA overlaid and cast to bf16) and `scripts/v12/mk_ema_unify17k.sh` | 02 §4.5; 03 §4.13; /fsx/home/weikai.huang/_lc/mk_ema_init.py (exists, 784 B) | **COLLAPSE** into the ckpt overlay + `tools/mk_ema.py` | train/ckpt.py, tools/ | UT: overlay == old tool output, bytewise | no |
| T-19 | (b) Stage chaining and compute: S2 initialises from S1 `checkpoint-20000_ema` (end of stable, not the decayed 26k); S1 lr 1e-4 / 26k; S2 lr 2e-5 / 106k; effective batch 256; SS at bs4 (bs8 OOMs, ~135 GB, with ROAD + live cond) | 02 §2, §4.5, §5 | **PRESERVE** in recipes | recipes/ | UT recipe → args parity | no |
| T-20 | (b) WSD: warm-up 100, stable to max − 6000, cosine to 0; a true resume restores the scheduler, `init_from` does not | 02 §4.6 | **PRESERVE** | train/schedule.py | UT LR curve | no |
| T-21 | (b) Optimizer: DeepSpeed AdamW β (0.9, 0.95), eps 1e-8, wd 0.01 on **all** params (LN, bias, adaLN; one group); ZeRO-1 `BF16_Optimizer` with fp32 master and fp32 grad accumulation (`deepspeed_zero1_fp32acc.json`); flows hard-cast to bf16 except complex rope | 02 §1.10, §4.11, §7.8; builder.py:22-56 | **PRESERVE** | train/trainer.py | G4 20-step curve | no |
| T-22 | (b) Elastic GC on shape/tex (`m.__class__` swap, `LinearMemoryController(1000, 500, 0.75, 0.5)`, state not saved); SS no GC; S3 static GC = 1.0 | 02 §4.9; 03 §1.3; TN:1381-1397 | **PRESERVE** (numerically neutral) | train/trainer.py | G4 equal with/without GC | no |
| T-23 | (d) `build_trellis_decoders` adds 3.06B frozen params (~6 GB on GPU) to every shape/tex job and writes them into every ckpt (1572 keys) | 02 §0.6, §3, §7.17; DESIGN #12; builder.py:158-179 | **DROP**: not built, not saved, ignored on load | models/towers.py | G4 unchanged; ckpt size | no |
| T-24 | (b) Training kernels: `ATTN_BACKEND=flash_attn_3` (sparse varlen), masked dense x-attn forced to sdpa, `FUSED_MODULATE=1`, `GEOTEX_XATTN_FLASH=1` in S3 only (flash when every key is valid, FH:1710), `COMPILE_SS=0` | 02 §4.10; 05 §4; train_native_split.sh:134-135; train_native_geotex.sh:156-157; v12_s3_4n.sh:32 | **PRESERVE** via `utils/backend.py`; record per ckpt | utils/backend.py | ports of test_xattn_flash / test_xattn_varlen | no |
| T-25 | (d) torch.compile path (`COMPILE_SS`, compile_mode/dynamic, accelerate patch); ROAD hooks forbid it | 02 §3, §4.10; TNV:839-844; TN:1352-1372 | **DROP** | — | — | n.a. |
| T-26 | (e) 120 `NativeArgs`: ~78 never exercised, ~35 constant, 7 per stage; launch fragility (duplicate `--flow_tune`, last wins; `EXTRA_ARGS` overriding `--save_steps`; in-repo `v12_ss_cont.sh` drifted from the one that ran) | 02 §2, §7.13; DESIGN §4 | **FIX**: recipes (7 fields + compute), constants hard-coded | recipes/, train/stages.py | UT recipe → args parity against every v12 launcher | n.a. |
| T-27 | (d) Dead trainer branches: flow-freeze sub-rules, inline live encode (`LIVE_COND_PREFETCH=0`) and its profiling, geotex anneal, `S3_TASK_DEBUG`, vggt REPA setup, MDS path, legacy dataset (`dataset_native` imported unconditionally, TN:322), compile patch, `_enforce_no_offload` | 02 §3; 04 §4 | **DROP** (keep the prefetch path; debug → flag) | train/ | import closure | n.a. |
| T-28 | (d) Dead model branches (TNV): VLM build, geotex MMDiT3D, slat-1024 builders, MLP connector, pos_stamp, learned view table, dino_align, dual_cond, distill, generic SS GC, vggt projector, ROAD GC, shape ROAD tap, depthwise routing, `encode_cond`, `_build_dino_anchor`, the inert `_repa_lambda` (still runs every step), LM-task path, diagnostics | 02 §3; TNV:297-1459 | **DROP** | models/ | — | n.a. |
| T-29 | (d) Dead loss / module branches: FH detach, pos_stamp, qwen_drop at p=0, distill, KD, vggt REPA, shape ROAD; loss.py KD; builder 1024 variants; `repa.py`; road `matcher_cpu`, `pool`, `evaluation`; tr2_modules `/weka` fallback | 02 §3 | **DROP** | losses/ | — | n.a. |
| T-30 | (d) 13 of 15 DeepSpeed configs are dead (keep `deepspeed_zero1_fp32acc.json` and `_universal`) | 02 §3; 05 §1 | **DROP** | recipes/deepspeed/ | — | n.a. |
| T-31 | (a) Logging calls `.item()` on every forward → forced GPU syncs | 02 §1.12; loss.py:268-269; FH:383-384,472 | **FIX**: accumulate on device, sync at log steps | train/trainer.py | G4 unchanged | no |
| T-32 | (f) Retention: `save_total_limit` rotation deleted the EMA-init source; fork_guard / ckpt-mapper / migrate scripts protect milestones; the S1 keep entries are symlinks into near-quota `/fsx/home` run dirs (verified `keep/v12/s1_connector/*` → `/fsx/home/…/runs/s3_*_v12s1b`) | 02 §0.3, §7.13; 05 §3a | **FIX**: `keep_steps` recipe field; runs_root on /fsx/data; copy S1 milestones into keep | train/ckpt.py, cluster/ | UT retention | n.a. |

## 3. Unified model and S3 loss (U)

| ID | item | evidence | disposition | BLIP3D owner | verification | compat_v12 |
|---|---|---|---|---|---|---|
| U-01 | (a) Inference ran the loop attention path while S3 trained fused: `fused_attn` defaults to False (UG:359), only TNV:416 sets True; the loop path rotates the geo key the tex lane reads (UG:839), so tex→geo tags differ. Published unify17k numbers are loop-path. The lc runners were patched 2026-09-24 07:45 (`--attn fused` default, lc/run_unify_bench.py:25,81; lc/unify_render.py:113), **the in-repo `scripts/v12/{run_unify_bench,unify_render}.py` were not** | 03 §0.1, §1.3, §7.1; DESIGN #1; scripts/g0_fused_attn.py:199-204; verified by diff | **FIX**: fused only; loop path kept as a test oracle; compat `attn=loop` reproduces the published numbers (DECIDE-4) | models/unified/model.py | G2 fused; RM sfv50 5 cases fused vs loop (G5) | yes |
| U-02 | (a) Refine and tex-on-GT-shape run `tex_forward_cached` → `_tex_block_inner` with no SS K/V, while the training tex lane always had SS keys (`_fusion_plan` tex lane = (True, True)) | 03 §0.2, §7.2; DESIGN #5; UG:224, 857-882, 1479-1498; GS:299-328 | **FIX**: `tex_given_mesh(..., z_ss)` passes SS K/V (generated z; GT SS latent for the controlled-texture track) | infer/unified.py, models/unified/model.py | RM A/B on sfv50 (G5) | yes |
| U-03 | (a) `precompute_geo_kv` uses `t_embedder(t_s)` only; training adds `cross_alpha_s · t_mixer_s(t_x)` (17k EMA `cross_alpha_s` = −1.16e-3), so the one-pass K/V cache is exact only if that term is 0 | 03 §0.3, §7.3; DESIGN #7; verified UG:1326 vs 1456 | **FIX**: include the term (geo pass per tex step, or a cache keyed by t_x) | models/unified/model.py | G2 cached path == full forward; RM cost | yes |
| U-04 | (a) The joint-loop uncond forward gives the SS lane the **positive** `cond_ss` (`ss_kw` GS:230-233 passed via `**kw` at GS:410-413), while training replays the tex lane's drops onto SS. The SS K/V cache (`cache_ss_kv`, GS:393) is exact only because both branches share `cond_ss`; after the fix it must hold two sets | 03 §0.4, §7.4; DESIGN #6; verified GS:230-233,393,410-413 | **FIX**: SS lane gets the uncond in the uncond branch; two cached K/V sets | infer/unified.py | G3; RM | yes |
| U-05 | (a) Geo lane never dropped in S3 (`joint_cond_drop` False) but joint inference applies shape CFG 7.5 with `uncond_s`. **New:** in training, tex-dropped rows read geo K/V computed from the **positive** geo cond (geo is never dropped); the joint-loop uncond forward computes geo from `uncond_s` and tex-uncond reads that | 03 §0.4; FH:1084-1105; GS:410-413 (new by reading) | **DECIDE** (DECIDE-3) | infer/unified.py, losses/unified.py | RM (3-forward variant) | yes* |
| U-06 | (a) Interleave lag pairing reads the mutable `SS_PARAMS` (training `_t_seq_len_probe` FH:905-910; GS:514 `S = p["steps"]`); the text bench sets `SS_STEPS=24` → k0/24 pairing never trained | 03 §0.5, §7.5; `scripts/v12/bench_unify17k_m12.sh:9` | **FIX**: constant `TRAIN_LAG_GRID` (12 steps, rescale 5/3) used by training and interleave | losses/unified.py, infer/unified.py | UT; RM text interleave | yes |
| U-07 | (c) Interleave reruns SS to recover z (GS:474-492); `sample_three_tower_interleaved` is monkeypatched onto the class | 03 §3.1 | **COLLAPSE**: round 1 returns z | infer/unified.py | G3 (z bit-equal) | no |
| U-08 | (b) SS←slat read (lag rows; interleave round 2): queries un-roped, no stream tag, per-head zero-init gate (17k mean abs 0.004) | 03 §1.3, §7.10; UG:1168-1227 | **PRESERVE** as trained, if interleave is kept (DECIDE-16) | models/unified/ss_lane.py | G2 interleave | no |
| U-09 | (b) Stream tags on the identity-pad rotation pair (geo 0, tex π/2, SS 3π/2), absolute (own q and k rotated); fused `_fusion_plan` (geo K = [geo;tex;SS] if t_s > 0 else [geo]); `ss_phases_slat` c → 2c+0.5 (non-persistent); parity asserts UG:286-322 | 03 §1.1, §1.2; UG:102-139,182-231,505-510,741-747 | **PRESERVE** verbatim | models/unified/{ops,plan,ss_lane}.py | G0-style UTs (fused vs reference softmax, corner bit-exactness, tag antisymmetry, SS re-rope) | no |
| U-10 | (b) `t_mixer` / `t_mixer_s` with zero-init `cross_alpha(_s)`; tex input = cat[x_tex; concat_cond] with coords asserted equal | 03 §1.1; UG:370-400,1304-1336 | **PRESERVE** | models/unified/model.py | G2 | no |
| U-11 | (b) Row classes and timesteps: clean .7 / lag .3 / solo 0; corners 0.1 / 0.2 rescaled to 0.143 / 0.286 inside clean; lag k0 ~ U{3..11} with shift5 / shift3; t_ss ≤ t_s ≤ t_x asserted; every primitive drawn for every row in fixed order (RNG discipline) | 03 §2.3; FH:682-902 | **PRESERVE** | losses/unified.py | UT distribution; G4 unified | no |
| U-12 | (b) Placeholder tex rows: t_x forced to 1, tex voxels removed from the loss, `t_x_sampled` logged; SS_DUAL = second `ss_forward` on clean rows with t2 = sigmoid(1+N(0,1)) under autocast; tex concat = the noised geo state x_ts (the two norm sets are bit-identical) | 03 §2.3; FH:1157-1160,1210,1251-1258,1312-1343 | **PRESERVE** | losses/unified.py | ports of test_tex_mask / test_ss_dual; G4 | no |
| U-13 | (b) Loss terms: tex `_voxel_balanced_mse` with a global all_reduce called unconditionally; SS `_row_balanced_mse` (¬clean) + dual (clean); geo on (t_s ≠ 0) ∧ ¬solo, weight 1; t_x = 1 rows not masked for tex | 03 §2.4; FH:945-976,1264-1287,1534-1570 | **PRESERVE** (collective discipline) | losses/unified.py | port test_sync_ddp; G4 | no |
| U-14 | (b) `probe_every=200` cond-sensitivity probe is live: two extra no-grad forwards at t=1 with rolled cond, SS lane omitted | 03 §2.4; FH:1634-1687 | **PRESERVE** as an explicit option (v12 default on); check whether it consumes CUDA RNG — if so it is part of the draw order | losses/unified.py, eval/probe.py | UT RNG state unchanged by the probe | no |
| U-15 | (e) The geo loss is active only through `UNFREEZE_GEO` (TNV:1249-1252) while `all_trainable` makes `unfreeze_geo` a no-op; the S3 knobs (P_*, SS_DUAL, GEO_LOSS_W, COUPLING, …) live as env in a 231-line shell | 03 §1.3, §2.1; 05 §5b | **FIX**: `recipes/s3_unify.yaml` fields (`trainable: all`, `geo_loss_w`) | recipes/, train/stages.py | UT recipe → args parity | n.a. |
| U-16 | (d) Dead unified options: gated coupling, cond-stream mode, one-way pair, `xattn_scale` anneal, `from_scratch` / MMDiT3D (`mmdit3d.py`, 810 lines), per-stream stamp tables on the model, `stage1/2_trainable_parameters`, the `unfreeze_geo` train() override, `test_identity_mask` (test-only) | 03 §1.3, §1.4 | **DROP** | — | — | n.a. |
| U-17 | (d) Elastic memory controller on the unified model (dead in v12; it would silently drop `ss_kv_cache`, UG:1283-1286) | 03 §1.3, §7.14 | **DROP** | — | — | n.a. |
| U-18 | (d) Two-tower samplers and loaders: `sample_mesh_only(_marginal)`, `sample_joint` (α=∞), `assemble_unified(_from_run)`, `_from_pretrained_pair`, `assemble_unified_inference`, `load_unified_inference`, `load_connectors` | 03 §1.3, §3.1; UG:1559-1579,1676-1803; GS:238-365 | **DROP** | — | — | n.a. |
| U-19 | (d) Dead loss parts: from_scratch branch, mismatch hinge, unified-model stamp logs, `ss_flow_present=False` branch | 03 §2.5 | **DROP**; keep `joint_cond_drop` (pending DECIDE-3) and `p_solo` (knob at 0) | losses/unified.py | — | n.a. |
| U-20 | (a) Skeleton assembly wastes I/O: `assemble_unified_tri` strictly loads all three specialist weights, then the runners overwrite them with `unified_geotex.*` | 03 §3.2; UG:1582-1628; lc/run_unify_bench.py:77-81 | **FIX**: skeleton from TRELLIS.2 json configs + `_uniform_bf16`, then strict load | models/unified/loading.py | UT strict key sets | no |
| U-21 | (a) `_connector_from_cfg` / `load_run_connectors` fail on the S3 ckpt: its config says `cond_seg_embed=False, cond_patch_pos='off'` but all three connectors carry BF16 stamp buffers (verified in config.json and the safetensors header), and `ss_connector` is never loaded | 01 §4.11, §6.4 ([unverified by execution]); 03 §0.8, §3.5.4; DESIGN #9; UG:1650-1673,1773-1783 | **FIX** + **COLLAPSE** the 4 builders into `connector_from_state` (reference lc/run_unify_bench.py:62-76 + benchmarks/checkpoint.py:39-106): infer n_blocks and stamps from keys, then cross-check with config | cond/connector.py, models/unified/loading.py | UT strict load of every keep/v12 ckpt | no |
| U-22 | (a) The unify runner reads `model.safetensors` only (no EMA overlay or check) and trusts the `_ema` dir; status.json says "EMA-overlaid" regardless | 03 §3.2, §4.13, §7.12; lc/run_unify_bench.py:26,64,125 | **FIX**: `load_state(use_ema, require_ema)`; record the actual weight source | train/ckpt.py, infer/ | UT | no |
| U-23 | (f) Uncommitted working-tree code backs the live runners: `precompute_ss_kv`, `JOINT_OPTS.cache_ss_kv` (UG/GS) and the EMA init (TN); `cache_ss_kv` exactness is claimed by construction, never executed | 03 header, §7.13; 05 §0.2; verified `git status` | **PRESERVE** as spec (commit before freeze) | infer/unified.py | G3 cache on == off | no |

## 4. Inference (I)

| ID | item | evidence | disposition | BLIP3D owner | verification | compat_v12 |
|---|---|---|---|---|---|---|
| I-01 | (b) Time grid `t_seq = r·u/(1+(r−1)u)`, u = linspace(1, 0, steps+1); the model is called at the first `steps` nodes with t·1000 in fp32; Euler x ← x − (t − t_prev)·v | 03 §4.1; GS:67-71 | **PRESERVE** | infer/schedule.py | UT vs TRELLIS `flow_euler.py`; G3 | no |
| I-02 | (b) Guidance interval inclusive on the node value; outside it the conditional prediction alone; in the joint loop tex guidance is evaluated at the warped t_x, and TEX steps / rescale_t are ignored (tex follows the shape grid; they matter only for refine and the separate tex tower) | 03 §4.2; GS:91-92,386-387,415-417 | **PRESERVE**; document | infer/schedule.py | UT | no |
| I-03 | (b) CFG rescale uses one scalar population std over all voxels and channels; a per-row std on (N,C) is a different operator (bug class of 2026-08-17); on dense SS it equals the official sampler | 03 §4.3; GS:93-108 | **PRESERVE** | infer/schedule.py | UT vs `SparseTensor.std` | no |
| I-04 | (b) `_guided` shortcut: strength 1 → skip the uncond forward (refine); the joint loop always runs both | 03 §4.4; GS:323-324 | **PRESERVE** | infer/schedule.py | G3 | no |
| I-05 | (b) Alpha warp t_x = αt/(1+(α−1)t), α = 32, shape leads; the last joint step covers ~90 % of the tex trajectory (why refine exists) | 03 §4.5; GS:112-124 | **PRESERVE** | infer/schedule.py | UT | no |
| I-06 | (b) Seeds: SS `Generator(seed)` (the truncation randperm uses the same generator); joint = new `Generator(seed)` → x_s then x_x; refine = seed+1 (GS:425); interleave round-2 noise from the round-1 generator; the separate cascade uses `seed` for SS, shape and tex | 03 §0.6, §4.6; DP:280,359-360,390-392 | **PRESERVE** per mode (a documented unify-vs-cascade confound) | infer/ | G3 | no |
| I-07 | (a) Voxel cap differs by path: unify randomly subsamples > 8192 voxels (reported `truncated`), the separate cascade never truncates; how often it fired on sfv50 was never tallied | 03 §0.6, §4.7, §7.6; GS:219-225 | **FIX**: `max_voxels` preset field (v12 values: unify 8192, cascade none); RM tally `truncated` in the existing status.json files | infer/presets.py, infer/stages.py | RM | no (default = v12) |
| I-08 | (b) Empty SS → `info.empty`, the caller writes error.json, never a stub asset | 03 §4.7; GS:212-218; lc/run_unify_bench.py:128-130 | **PRESERVE** | infer/stages.py | UT | no |
| I-09 | (b) Occupancy: SS latent raw; `ssdec(z) > 0` at 64³ with the TRELLIS **v1** `ss_dec_conv3d_16l8_fp16`; coords = max_pool3d(2) > 0.5 → argwhere → batch column 0, int32 | 03 §4.8; occ_metrics.py:71-99; export_glb_fullchain.py:34-35 | **PRESERVE**; decoder path from paths.yaml | infer/stages.py | UT; G3 coords | no |
| I-10 | (c) The SS noise → occupancy → coords chain is written 4× | 03 §3.5; DP:286,349-352; run_sep_text.py:69-73; occ_metrics.py:71-99; export_glb_fullchain.py:137-150 | **COLLAPSE** → `occ_to_coords` | infer/stages.py | G3 | no |
| I-11 | (c) SS samplers: official `FlowEulerGuidanceIntervalSampler` (separate path) vs the hand loop (GS:191-203, again GS:477-490); equivalent on dense tensors | 03 §3.1, §3.5 | **COLLAPSE** → `sample_ss` on `_guided` | infer/stages.py | G3 cascade SS latents equal | no |
| I-12 | (c) Shape/tex drivers: `sample_shape` (eval_fusion_v22.py:120-152), `sample_tex` (eval_tex_v22.py:60-89) vs the unified `sample_tex_given_mesh` | 03 §3.1, §3.5 | **COLLAPSE** → `euler_cfg(forward, x0, preset, seed)` | infer/schedule.py, infer/cascade.py | G3 | no |
| I-13 | (a) Separate-tower loaders use `strict=False` and only print the missing count (shape/tex; SS asserts); the towers are evaluated in the `from_pretrained` mixed layout (fp32 boundary layers) although trained all-bf16, while unify is evaluated in the training bf16 layout under autocast | 03 §3.2, §4.12, §7.8, §7.11; verified eval_fusion_v22.py:41-55, eval_tex_v22.py:45-57, export_glb_fullchain.py:38-52 | **FIX** strict loading; **PRESERVE** the layout per path as an explicit preset field (v12 values); RM layout swap on 5 cases | models/towers.py, infer/presets.py | UT strict; RM | no (default = v12) |
| I-14 | (a) Eval attention backend: every v12 bench / demo entry point sets `ATTN_BACKEND=flash_attn` (FA2) and `FUSED_MODULATE=1`; training uses FA3; a bare `_paths` import defaults to sdpa. **Corrects** DESIGN §8 and 05 §4 ("sdpa for eval") | 05 §4, §5a; verified lc/run_unify_bench.py:13, lc/run_sep_text.py:8, lc/unify_render.py:10, scripts/demo_app.py:21, scripts/run_demo_app.sh:77, eval_suite run_v12_image.py:25, _paths.py:26 | **FIX**: backend chosen per purpose in `utils/backend.py` and recorded; eval default FA2 (v12) | utils/backend.py | G2 per-backend tolerance | no (default = v12) |
| I-15 | (e) Mutable global presets: `GS.SS_PARAMS` read live (also by the training lag grid), SHAPE/TEX copied at sampler construction, the demo swaps `TEX_PARAMS` under a lock, runners mutate them from env (SS_* / SHAPE_* / TEX_* / TEX_GI / SS_GI) | 03 §3.5, §4.15; DP:384-395; lc/run_unify_bench.py:50-58 | **FIX**: frozen `Preset`s — image: SS 12 / 7.5 / 0.7 / [0.6,1] / 5, shape 12 / 7.5 / 0.5 / [0.6,1] / 3, tex 12 / 1.0 / 0 / [0.6,0.9] / 3; text: SS 24 / 10, tex 24 / 7.5 / 0.5 / [0.9,1] / 5 (demo and bench agree, verified DP:76-89 vs bench_unify17k_m12.sh:9) | infer/presets.py | UT presets == v12 values; G3 | no |
| I-16 | (e) Mode numbering conflicts: GS docstrings ① joint ② tex-given-mesh ③ mesh ④ three-tower vs bench refine = 3, joint = 2, interleave = 1 | 03 §6; GS:152,237,298,330; lc/run_unify_bench.py:24 | **FIX**: names `cascade / refine / joint / interleave` | infer/ | — | n.a. |
| I-17 | (c) Mesh / GLB helpers: 3× shape GLB ([x, z, −y] swap), 2× voxel cubes, `build_mw` vs `render_textured` (`build_mw` swallows `fill_holes` failures) | 03 §3.3, §3.5 | **COLLAPSE** into export.py; **FIX** log `fill_holes` failures | infer/export.py | UT GLB on fixed latents | no |
| I-18 | (b) GLB export: `to_glb(decimation 200000, texture 2048, remesh True, band 1, project 0)`, webp; no `mesh.simplify(16_777_216)` (the official runner does it); `shape_dec.set_resolution(512)`; tex decoder ·0.5+0.5; MeshWithVoxel origin −0.5, voxel 1/512 | 03 §3.3; export_glb_v22.py:31-54 | **PRESERVE** (v12 = no simplify) | infer/export.py | G5 | no |
| I-19 | (b) GLB / UV / axes: glTF UV origin top-left; `shape.obj` decoder-native Y-up; `shape.glb` and voxel GLBs apply [x, z, −y]; PBR voxel layout base 0:3, metallic 3, roughness 4, alpha 5; every new GLB consumer runs `uv_flip_probe` | 03 §4.14; memory suite-uv-convention-bug | **PRESERVE** | infer/export.py, eval/suite.py | `uv_flip_probe` on BLIP3D GLBs | no |
| I-20 | (b) Flat-colour assets have darker / more saturated GT by the official toolkit convention; a fix exists, the user decision is pending | 03 §4.14; memory flat-colour-factor-convention | **PRESERVE** until DECIDE-19 | eval/ | — | n.a. |
| I-21 | (d) Legacy samplers: export_glb_fullchain `sample_ss` (25 steps, cfg 3, no interval), `eval_render.sample_slat` (25 / 3.0) | 03 §3.1 | **DROP** | — | — | n.a. |
| I-22 | (a) eval_suite `run_v12_image.py` is broken against the current demo. **Verified** (03 had it [unverified]): since d8d76dd `generate()` no longer calls `build_stage_cond`, so the monkeypatch is a no-op, and `_cond` dereferences `pack.rec` on `pack=None` (DP:101,319-324) | 03 §0.7, §7.7; eval_suite run_v12_image.py:40-42,65 | **FIX**: thin adapter over `blip3d.infer.Pipeline`; the sep106k image results came from the 2026-09-11 package copy in lc/pristine_repo (E-12) | eval/suite.py | G5 re-score 5 cases | n.a. |
| I-23 | (a) eval_suite `run_s3_t50.py` drives v12 weights through the pre-CondStamp `live_cond.build_stage_cond` | 05 §3c; DP:49-55 | **DROP** / replace with the BLIP3D adapter | eval/suite.py | — | n.a. |
| I-24 | (f) `scripts/` is used as a library (`eval_fusion_v22` imported 101×, `export_glb_fullchain` 51×, `eval_tex_v22` 27×) via `sys.path.insert(REPO)` | 05 §0.4, §2a | **COLLAPSE** into `blip3d.infer`; eval_suite imports the installed package | infer/ | import test from eval_suite | n.a. |
| I-25 | (a) **New**: import-time env side effects — `export_glb_fullchain.py:11` setdefaults `EVAL_COND_ROOT` to a v2.2 cache and :33 reads it; `eval_fusion_v22.py:27` binds `COND_ROOT` at import; `live_cond.py:54` binds the cond VLM at import | not in reports (verified) | **FIX**: no env reads at import in package code | utils/ | UT importing blip3d leaves `os.environ` unchanged | n.a. |
| I-26 | (e) Demo defaults: `FINALS=/fsx/data/weikai.huang/runs`, `DEMO_{SS,SHAPE,TEX}_CKPT`, `vlm=V22_CKPT`, `setdefault(COND_VLM_CKPT, stock)` | 03 §3.4, §7.15; 05 §5a; DP:35,42-45,64-66,183 | **FIX**: `recipes/infer/*.yaml` + paths.yaml | infer/, recipes/ | — | n.a. |
| I-27 | (b) Product weights = EMA overlaid on the model in bf16; EMA holds no buffers (stamps, rope_phases, view codes) | 03 §4.13; benchmarks/checkpoint.py:17-28; UG:1550-1552 | **PRESERVE** | train/ckpt.py | UT | no |

## 5. Data (D)

| ID | item | evidence | disposition | BLIP3D owner | verification | compat_v12 |
|---|---|---|---|---|---|---|
| D-01 | (b) Rank-synced task draw: `task_seed = (base·1_000_003) ^ (worker+1)`, no rank term; per-batch params (IM n) from the same `task_rng`; relies on equal `num_workers`, torch's in-order worker round-robin and no per-rank consumption, else DDP hang or silent gradient corruption | 04 §5.1; MX:166-167,211-213; rank_aware.py:34-44 | **PRESERVE** + runtime assert (all-gather the task id every N steps; replaces `S3_TASK_DEBUG`) | data/mixture.py | UT; multi-rank smoke | no |
| D-02 | (b) Per-rank index streams: `within_seed = base·1_000_003 ^ rank·9176 ^ (worker+1)`, per task `^ (i·7919+1)`; each worker keeps its own permutation and burns other workers' indices (1/W thinning); effectively sampling with replacement across (rank, worker) | 04 §5.2; MX:171-175,193-204,239 | **PRESERVE** (a true partition is a later option) | data/mixture.py | UT index streams == old | no |
| D-03 | (a) Per-item RNG is unseeded `np.random.default_rng()`: view, caption tier, IM combo, resample target, ROAD points | 01 §2.27, §6.9; 04 §5.3; TH:441,448; TT:70; road/points.py:14-19 | **FIX**: seed from (base, rank, worker, step, slot); compat = unseeded (DECIDE-14) | data/task.py | UT item reproducible | yes |
| D-04 | (b) accelerate `IterableDatasetShard` pass-through patch (accelerate 1.7.0 internals); `dispatch_batches=False`; no sampler / worker_init_fn, prefetch 2, fork, pin_memory; `--max_steps` mandatory; `remove_unused_columns=False` | 04 §1.1, §5.4-5.6, §7.12; rank_aware.py:67-94; TN:1034,1450,1459-1463 | **PRESERVE** + version assert (or own the DataLoader, DECIDE-2) | data/dist.py | UT patch active | no |
| D-05 | (a) Manifests held as per-row Python dicts (~3.9 KB/row; ~14 GB per rank; pool1800k loaded twice, for I1 and IM; COW pages dirtied) [spec, RSS unmeasured] | 04 §2.1, §5.7, §7.4 | **FIX**: columnar manifest store shared between tasks | data/manifest.py | RM RSS | no |
| D-06 | (b) Filters: `min_aesthetic` 4.5 keeps None-score rows (~53 % of capT1800k; 1,469,558 → 1,446,027); `require_caption` for T | 04 §2.1; TH:165-188 | **PRESERVE** (explicit `keep_unscored: true`) | data/manifest.py | UT counts == S3 log | no |
| D-07 | (a) Voxel cap enforced lazily (exception → resample) and **not at all for the SS tower** (`ss_only` returns before the cap, TH:628-630; 1.2 % of assets exceed 8192). DESIGN #10 merges this with D-08; they are different issues | 02 §1 step 2, §7.11; 04 §2.5; DESIGN #10; TH:641-647 | **DECIDE** the SS-tower rule (DECIDE-5); **FIX** mechanism: `n_vox` manifest field, cap filtered at load per target | data/manifest.py, data/latents.py | UT pool counts; G4 | yes* |
| D-08 | (a) `TargetBatcher` tail starvation (S3): items with cost > ~13.9k (vox > ~11.8k) are never popped (32/96 slots stuck after 6k batches, simulated); items at 8193-11.8k are popped, fail the cap, and are replaced by a uniform random index (un-balancing the slot); the "consumed exactly once" docstring is false | 04 §5.8, §7.3; balanced_assign.py:79-81,96-118 | **FIX**: pre-filter over-cap items + age-based forced pop | data/balance.py | UT simulation: no stuck slots | yes |
| D-09 | (e) Balance table: env `GEOTEX_BALANCE_TABLE=voxels_v11.npz`, S3 only (the brief's "S1/S2 used it" is wrong); built by untracked `scripts/_tmp/build_voxel_table.py` walking latent dirs; a missing sha silently falls back to the median; must be rebuilt whenever the pool changes | 02 §0.4; 04 §3.2, §5.8, §7.14; 05 §0.3; MX:154; memory voxel-table-follows-the-data | **FIX**: `n_vox` from the manifest; coverage asserted | data/balance.py, tools/ | UT coverage 100 % | no |
| D-10 | (b) Balance cost model: cost = median + vox; per-task target = per_batch·(median + task mean); buffer max(24, per_batch·`GEOTEX_BALANCE_BUF`=24) = 96 at bs4; seed rank·7919+13; greedy seed + 400 random k-combos; ranks independent | 04 §5.8; MX:227-228,366-377 | **PRESERVE** (recipe `balance:` block) | data/balance.py | port test_balance (off ≡ bit-identical) | no |
| D-11 | (a) Broad `except Exception` resample: 8 guarded tries to a uniform unseeded index (bypasses the permutation and the balancer), 9th unguarded; 6,561 resamples in the S3 r0 log (mostly the cap); non-cap: EOFError ×8, missing renders ×7, UnidentifiedImageError ×1, coords mismatch ×2 | 04 §2.8, §7.1; DESIGN #11; TH:428-444 | **FIX**: whitelist (IO / corrupt / cap), per-task counters, alarm on rate, fail fast on repeated identical errors | data/task.py | UT injected errors | no |
| D-12 | (b) Collate homogeneity: one `_task` per batch (asserted); shape gated on batch[0]; tex all-or-none (mixed batch drops tex, warns once); align_points all-or-none; T rows carry no DINO and `encode` / `_pack_live_conds` key on element 0 | 01 §2.28; 04 §1.3, §5.9; VCOL:195-249; MX:261-267; LCB:521,536; TN:428-447 | **PRESERVE** + assert modality homogeneity | data/collate.py | UT | no |
| D-13 | (a) The shape tower loads pbr for nothing (~25 % of 1800k rows lack it, so mixed batches drop tex; wasted I/O); `vlm_collate.py:224-228` comment "every row has pbr" is wrong | 04 §2.4, §7.7, §7.9 | **FIX**: `targets: [ss, shape]` + `tex_missing: placeholder / skip` | data/task.py, data/latents.py | G4 shape unchanged | no |
| D-14 | (b) Latent loading: SS npz key `z` (fallback `latent`), (8,16,16,16), no normalization; shape coords uint8 → int32, feats (N,32) in shape_norm; pbr in pbr_norm; tex concat = shape feats in `shape_slat_normalization` (bit-identical to shape_norm); coords equality asserted | 04 §2.4; TH:300-337 | **PRESERVE**; read the shape npz once for shape and tex | data/latents.py | UT | no |
| D-15 | (a) `load_norm_stats` silently returns None when a TRELLIS config is missing (config root derived from the repo parent, with a `/weka` fallback) → silent un-normalised training or inference | 02 §4.12, §7.15; 03 §3.2, §7.15; 04 §2.4, §5.12; tr2_modules.py:153-195 | **FIX**: hard fail (SS explicitly none); stats hashed into the ckpt config | data/latents.py, models/towers.py | UT missing file raises | no |
| D-16 | (b) Tex placeholder (S3): zeros x_0 on the shape coords, concat = shape feats (shape_norm), `tex_valid=False`; real pbr rows True; ~75 % pbr coverage | 04 §2.6; TH:657-682 | **PRESERVE** (`tex_missing: placeholder`) | data/latents.py | port test_tex_mask | no |
| D-17 | (b) ROAD align points: env `ROAD_ALIGN_POINTS` (10000) read in workers; voxel centres of the `shape_latent_512` coords in [−0.5,0.5]³ (pinned to 32³), with replacement if N < 10k, unseeded; raises without a shape latent, so SS rows lacking one are resampled away; bf16 after the HF cast (T-09) | 02 §1, §7.11; 04 §2.9; TH:597-624; road/points.py:14-19 | **PRESERVE**; pass the count explicitly | data/latents.py, losses/road.py | port test_road_wiring | no |
| D-18 | (b) Mixture: T real .6 / T synth .1 / I1 .15 / IM .15; IM n ∈ {2,3,4} p = [.15,.25,.6] locked per batch; IM view weights [.15×3, .6×2, 1×7, .5×4], distinct, sorted; I1 uniform over views 0-15 including below-ground (eval: `input_view_index` or `good_view` 5-11); S1 mixes == S2 texfixed mixes (verified diff); tex tower uses pbr-only pools; S3 adds `tex_placeholder` | 01 §2.26, §6.7, §6.14; 02 §4.14; 04 §0, §2.2; LCB:64-67,192-202; TH:222-233,464; EC:65-68 | **PRESERVE** (`recipes/data/*.yaml`); I1 view law → DECIDE-7 | recipes/data/, data/views.py | UT yaml → identical task args | no |
| D-19 | (a) Missing views: I1 falls back to the first sorted render (view id taken from the filename); IM silently drops missing views, so a row can carry fewer views than the batch lock | 01 §2.26; 04 §2.2, §7.5; LCB:168-174,219-227 | **FIX**: keep the behaviour, count and log it | cond/prep.py | UT | no |
| D-20 | (e) Manifest schema: hex sha (64 / 32 / 20-hex; synth is its own namespace), captions order [long, medium, short, long+texture], empties filtered (would shift tier meaning), per-modality required fields (`renders_dir` absent on synth), `n_views` default 16 | 04 §3.1, §7.15 | **FIX**: validate at load, loud counts | data/manifest.py | UT | n.a. |
| D-21 | (d) `shape_latent_1024` is only partly populated; `slat_resolution: 1024` would silently drop tex and change the pool | 04 §3.1, §7.8 | **DROP** the 1024 option (validate 512) | data/manifest.py | — | n.a. |
| D-22 | (e) Absolute manifest paths in the yamls and absolute asset paths inside the manifests; launcher comments say `*_evalclean`, the yamls use `*_texfixed_ff` | 01 §5; 02 §4.14; 04 §3.2, §3.3; 05 §5a | **FIX**: `${data_root}` interpolation + manifest registry (DECIDE-12) | recipes/data/, utils/paths.py | UT resolve | n.a. |
| D-23 | (f) The texfixed manifests are built by out-of-repo `_lc/texswap/mk_texfixed_manifests.py`; lineage evalclean → texfixed (renders → `renders_cond_refix`, new captions) → `_ff` (pbr → `_flatfix`) is from memory, not re-verified | 04 §3.2; 05 §0.3, §3a | **FIX**: port to `tools/data/`; record lineage | tools/ | RM lineage check (DECIDE-17) | n.a. |
| D-24 | (a) Eval contamination: `texfixed_ff ⊆ evalclean` not re-verified; the eval sets have geometry-level duplicates in the training pool (8/50 exact geometry) | 04 §7.10; memory eval-geometry-duplicates | **DECIDE** (DECIDE-17) | tools/data/, eval/ | RM | n.a. |
| D-25 | (b) Renders come only from the manifest `renders_dir` (texfixed repointed to `renders_cond_refix` after the texture-swap bug); never glob render trees | memories renders-cond-stale-texture-bug, glob-on-renders-dirs-is-a-trap; 04 §3.3 | **PRESERVE** | data/task.py | UT (no glob) | no |
| D-26 | (d) Dead data modules: `base.py`, `granularity=item`, threed cache / packed / raw branches + `_load_views` + `collate_fn` kwargs + `GEOTEX_IM_VIEW_SUBSET`, `threed_balanced` + `category_balance`, `chat.py`, `streaming_task` (MDS), `packed_reader`, `tap_task` / `tap_ss_task`, `dataset.py` / `dataset_native.py`, `vlm_cache` except the pad helper, `balanced_partition` / `rank_slice` / `drain_ready`, dead `view_key` imports (TH:458) | 04 §4; 05 §7 | **DROP** | — | import closure | n.a. |
| D-27 | (c) Three task classes + `WeightedTextTo3DDataset` → one `ThreeDTask(modality)` | 04 §4, §6 | **COLLAPSE** | data/task.py | G4 | no |
| D-28 | (e) The mixture silently ignores the CLI data flags (`max_slat_tokens`, `ss_only`, `slat_resolution`); the effective 8192 is the TH:75 default | 02 §2, §7.12; 04 §0, §5.11; MX:334-343 | **FIX**: one source of truth (recipe data block) | recipes/, data/ | UT | n.a. |

## 6. Checkpoints and configuration (K)

| ID | item | evidence | disposition | BLIP3D owner | verification | compat_v12 |
|---|---|---|---|---|---|---|
| K-01 | (b) Key map, no file conversion: towers `diffusion_connector.*` (38), `ss_flow.*` (641 incl. C64 `rope_phases`, optional `_orig_mod.` infix), `shape_slat_512.*` / `tex_slat_512.*` (640), `repa_projector.*` (6), `_road_step`, `dino_view_embed`; unify `unified_geotex.{geo_flow, tex_flow, ss_flow, t_mixer(_s), cross_alpha(_s), ss_reads_gate}` + `geo_connector.` / `diffusion_connector.` (= tex) / `ss_connector.` (2047 tensors) | 02 §6; 03 §5.1, §5.2; DESIGN §7 | **PRESERVE** via a key map to `{ss,shape,tex}.{flow,connector}`, `road.*`, `view_code` | train/ckpt.py, models/unified/loading.py | UT strict load + key counts for every keep/v12 ckpt and the running SS-300k ckpt | no |
| K-02 | (d) `trellis_decoders.*` (1572 keys in shape/tex ckpts) | 02 §6; 03 §5.2 | **DROP** on import | train/ckpt.py | UT | n.a. |
| K-03 | (b) EMA files: fp32, trainable keys only (42 / 36 in S1; 682 / 676 in S2; 2039 in S3), same names as the model → overlay; `_ema` dirs are pre-folded to bf16 | 02 §6; 03 §4.13, §5.1 | **PRESERVE** | train/ckpt.py | UT overlay | no |
| K-04 | (e) `config.json` holds only the model config (110 keys; NativeArgs live only in `training_args.bin`) and carries misleading fields (`vlm_model`, S3 stamp flags) | 02 intro, §0.7; 03 §0.8; DESIGN #9 | **FIX**: BLIP3D config = model, version v12, stage, tower, full recipe, encoder ids + revisions, stamp mode / scale, backends, compat flag, norm-stat hashes; verified on load | train/ckpt.py | UT | n.a. |
| K-05 | (e) Checkpoint dirs are named by local step (T-05) | 02 §4.13; ss_to300k.sh:3-6 | **FIX**: global step names; a mapping table for old dirs in `docs/ckpt_format.md` | train/ckpt.py, docs/ | — | n.a. |
| K-06 | (b) Resume semantics: HF regex `checkpoint-\d+` (`_ema` dirs ignored); any `checkpoint-*` in output_dir triggers auto-resume | 02 §4.13; TN:1508 | **FIX**: explicit `resume:` recipe field, no implicit trigger | train/ckpt.py | UT | n.a. |
| K-07 | (b) Universal-checkpoint conversion for world-size changes (`ds_to_universal`, `deepspeed_zero1_fp32acc_universal.json`) | 05 §1, §3b; ss300k_failover.sh:22-38 | **PRESERVE** | train/ckpt.py, cluster/failover.py | 4n → 2n resume test | no |
| K-08 | (b) Old checkpoint content: model.safetensors (bf16 + I64 / C64), ema.safetensors, `global_stepN/` ZeRO shards, `rng_state_{rank}.pth`, trainer_state.json, training_args.bin, config.json, `latest`, zero_to_fp32.py | 02 §4.13 | **PRESERVE** reading; BLIP3D format documented in `docs/ckpt_format.md` | train/ckpt.py | UT | n.a. |
| K-09 | (b) Complex `rope_phases` must survive dtype casts (`to_bf16_keep_complex`; assert UG:311-314) | 03 §5.3; tr2_modules.py:33-57 | **PRESERVE** | models/towers.py | UT | no |

## 7. Environment, repository, tooling (E)

| ID | item | evidence | disposition | BLIP3D owner | verification | compat_v12 |
|---|---|---|---|---|---|---|
| E-01 | (f) The real TRELLIS.2 is the external fork `model/third_party_3d_gen/TRELLIS.2`, reached by parent-dir arithmetic; HEAD `2ed033d` is unpushed (`origin/blip3o-next` = `9cfb2b3`, verified); dirty data_toolkit edits and untracked `data_toolkit/datasets/`, `errors.txt`; `o_voxel` is pip-editable from it | 05 §0.1, §4; _paths.py:13-16 | **FIX**: push and pin as a submodule (DECIDE-11) | third_party/, env/ | env build from the lock | n.a. |
| E-02 | (b) Fork patches are load-bearing: cond_mask / attn_mask plumbing (`5f97b27`), fused Triton norm-modulate / gate-residual and fp32 segment-sum (`2ed033d`) | 02 §4.10, §7.14; 05 §0.1 | **PRESERVE** | third_party/ | G2 | no |
| E-03 | (d) In-repo `third_party_3d_gen/TRELLIS.2` (185 MB, stock `75fbf01` + DINOv3 patch) is on no sys.path | 05 §0.1, §1 | **DROP** | — | — | n.a. |
| E-04 | (e) `_paths.py` resolves TRELLIS root, checkpoints and data from the repo parent, setdefaults `ATTN_BACKEND=sdpa`, `CUDA_HOME`, `CPATH`; tr2_modules falls back to `/weka/...`; moving the repo breaks silently | 05 §4, §5a; _paths.py:13-40; tr2_modules.py:153-159 | **FIX**: `paths.yaml` (`BLIP3D_PATHS`); `backend.configure()` before the trellis2 import | utils/paths.py, utils/backend.py | UT | n.a. |
| E-05 | (f) numpy 2.2.6 from `~/.local` shadows the env's 1.26.4 (verified: `numpy.__file__` is in `~/.local`; with `PYTHONNOUSERSITE=1` → 1.26.4); every v12 run since ~09-01 used 2.2.6; no launcher sets `PYTHONNOUSERSITE` | 05 §0.6, §4; DESIGN #13 | **FIX**: `PYTHONNOUSERSITE=1` everywhere + lock numpy to the version the goldens are recorded with (DECIDE-10) | env/, cluster/ | G1 / G4 under the lock | no |
| E-06 | (f) `requirements.txt` (torch 2.3.0, transformers 4.51.3) and `requirements-freeze.txt` (4.52.4) disagree with the live env (5.2.0); TN:749-750 comment "needs 4.57.6" is stale | 05 §1, §4 | **FIX**: regenerate the lock (`pip freeze` under `PYTHONNOUSERSITE=1`) + `env/BUILD.md` for FA3, flex_gemm, o-voxel, cumesh, nvdiffrast, spconv | env/ | env rebuild smoke | n.a. |
| E-07 | (b) Version-coupled behaviour: transformers 5.2.0 (hidden-state tie, DINOv3 `self.layer`), accelerate 1.7.0 (shard patch), Pillow 11.3.0 (banker's-rounding crop), deepspeed 0.19.0 (`BF16_Optimizer` path), torch 2.6.0+cu124 | 01 §2.8, §2.12; 04 §5.4; 05 §4; verified versions in the env | **PRESERVE**: pin + runtime version asserts | env/, utils/ | UT version asserts | no |
| E-08 | (f) `blip3o/model/__init__.py` eagerly imports the 2D AR models (→ `blip3o_arch` → `ta_tok` → `tok/`); the 3D code reaches `trellis_native_vlm` and `multimodal_decoder/builder` through it | 05 §0.5 | **FIX**: port the two files, cut the chain | models/ | import-time test | n.a. |
| E-09 | (f) Dirty working tree = live-run code: TN (+33, EMA init), GS (+9, `JOINT_OPTS`), UG (+33/−8, `precompute_ss_kv`), `scripts/v12/monitor_sscont.sh`; byte-identical to `lc/patched/*` | 03 header; 05 §0.2; verified `git status` | **FIX**: commit after SS-300k finishes (freeze list) | — | — | n.a. |
| E-10 | (f) Load-bearing but untracked / ignored: `tests/` (`.gitignore:32`), `scripts/_tmp/{eval_v10_tasks,_hf,build_voxel_table}.py` and ~29 `test_*.py` (ignored), `_lc` S1/S2 launchers, `_lc/mk_ema_init.py`, `_lc/texswap/mk_texfixed_manifests.py`, `_lc/balance/voxels_v11.npz`, the S3 mix yaml under `lc/configs` | 05 §0.3, §2a, §3; verified `git status --ignored` | **FIX**: force-add / snapshot before freeze | tests/, tools/ | — | n.a. |
| E-11 | (f) `scripts/v12` copies are older than the lc copies (`run_unify_bench` lacks `--attn fused`; `hold_reservation`, `launch_ss_cont`, `v12_ss_cont`, `monitor_sscont`, `sscont_kill`); lc-only ops scripts (`ss_to300k`, `ss300k_failover`, `cleanup_v12`, `timing_*`, `demo_smoke_v12`, `u17_*`, …) | 02 §3; 05 §2; verified diff of run_unify_bench | **FIX**: take the lc versions into the archive before freeze | cluster/, tools/ | — | n.a. |
| E-12 | (f) `lc/pristine_repo` is a hybrid: top level (incl. `.git`, `scripts/`) are symlinks into the live repo, but `trellis2_blip3o/` is a real 2026-09-11 copy (its `demo_pipeline.py` differs from HEAD). 05 says "not a copy — delete"; 03 calls it the older demo copy. It is the only copy of the package that produced the sep106k image results | 03 §0.7; 05 §3b; verified | **PRESERVE**: archive the package copy (tar or commit on a branch) before any cleanup | — | — | n.a. |
| E-13 | (f) eval_suite: **the 2026-09-02 UV-convention fix in `scripts/prepare_trellis2_e2e.py` is uncommitted** (git status `M`); `text/`, the `run_v12_*` scripts and the v12 configs are untracked | 05 §3c (UV fix not mentioned); memory suite-uv-convention-bug; verified | **FIX**: commit and tag before freeze | eval/suite.py (adapter side) | — | n.a. |
| E-14 | (e) Assets spread over two HF caches (TRELLIS-image-large SS decoder under `~/.cache/huggingface`), TRELLIS.2-4B as plain files, Uni3D glob, DINO mirror, BiRefNet, ULIP-2 in eval_suite | 05 §4 | **FIX**: paths.yaml + `tools/fetch_assets.py` verifying snapshot hashes; merge the caches | utils/paths.py, tools/ | fetch_assets check | n.a. |
| E-15 | (e) Env vars that change numbers (`COND_VLM_CKPT`, `GEOTEX_IM_TOK_PER_VIEW`, `VIEW_EMBED_SCALE`, `DINO_SIZE_IM`, `GEOTEX_BALANCE_TABLE/BUF`, `GEOTEX_XATTN_FLASH`, `COMPILE_SS`, `ROAD_ALIGN_POINTS`, `ATTN_BACKEND` / `SPARSE_*` / `FUSED_MODULATE`, `USE_EMA`, runner `SS_*` / `SHAPE_*` / `TEX_*`) mixed with path / debug knobs (`HF_HOME`, `LIVE_COND_SHM/PREFETCH/PROF`, `S3_TASK_DEBUG`, `DEMO_*`) and dead ones (`REPA_*`, `DUAL_CKPT`, `IM_VIEWS`, `POOL_SCALE`, `EVAL_COND_ROOT`) | 05 §5b; 02 §4.10 | **FIX**: numbers → recipe fields stored in the ckpt; env only for paths and backends; drop dead ones | utils/, recipes/ | UT: no numeric env read in the package | n.a. |
| E-16 | (e) Hardcoded absolute paths in package code (v2.2 default, `DPOS_NPZ`, tap tasks under `/opt/dlami/nvme`, demo `FINALS`, `/dev/shm/v22ckpt_live`, teacher glob, `blip3o/data/dataset.py:175` other user's paths, TN `data_path`, the scripts' cache roots / font / `SSDEC`) | 01 §5; 05 §5a | **FIX**: none in the package (DESIGN §3 rule) | utils/paths.py | CI grep for `/fsx`, `/weka`, `/opt`, `/dev/shm` | n.a. |
| E-17 | (d) Archive-only top level: `tok/`, `trl/`, `train.py`, `inference.py`, `gradio/`, `figure/`, `experiment/`, `build_index.py`, `setup.py`, `docs/` (cherry-pick 2), `benchmarks/` except `checkpoint.py`, `third_party/` eval (~13 GB), `checkpoints/uni3d-b`, `data/benchmarks`, `pix/`, `pixfix/`, `wandb/`, `.compile_cache/`, junk | 05 §1 | **DROP** from BLIP3D | — | — | n.a. |
| E-18 | (d) ~390 archive-only scripts (profiling, ablations, probes, earlier launchers, `_tmp` scratch); modules never reached (`codebook_prep`, `lora`, `mmdit_ss`, `plkv_ss`, `data.tap_ss_task`) and option-gated ones (`mmdit3d`, `mmdit_slat`, `plkv_slat`, `dual_cond`, `depth_fusion`, `repa`, `pos_stamp`, `dino_align` head) | 05 §2c, §7 | **DROP** | — | import closure of BLIP3D | n.a. |
| E-19 | (f) Tests: port `test_road_align` (skips without `_lc/third_party/ROAD` @c847391), `test_road_wiring`, `test_unified_conventions` (needs a 3-tower SS-lane update), `test_v12_assembly`, `test_demo_v12`, `test_geotex_init_guard` (reads `lc/patched/train_native.py`; repoint), the `_tmp` v12 unit tests; archive `test_repa_units` and the v10 tests; TN:49 cites a non-existent `tests/test_native_infer.py` | 01 §3; 05 §6 | **FIX**: port under `tests/unit` and `tests/gpu` | tests/ | CI | n.a. |
| E-20 | (f) Sibling worktrees `../BLIP3o-edit` (`s3-edit`; the edit-pair data), `../BLIP3o-rl` (`s3-rl`), `../BLIP3o-unionpre` (detached) share the `.git` | 05 §1; memory edit-pairs-already-built | **PRESERVE** the branches in the freeze (push / tag) | — | — | n.a. |
| E-21 | (f) Cluster ops semantics: heartbeat (lc `hold_reservation.sh` writes `<ISO Z> pid=`; `.reserved` expires in 20 min), monitors (stall = no training process, never log mtime), failover, `qwrite` for the quota, kill helpers as script files (pkill through srun kills its own step), hold rotation | 05 §3a, §3b, §7; memories | **PRESERVE** in `cluster/` | cluster/ | dry-run tests | n.a. |
| E-22 | (e) Runtime env: inductor / triton caches on `/dev/shm`, `TORCH_EXTENSIONS_DIR`, `FLEX_GEMM_AUTOSAVE_AUTOTUNE_CACHE=0` (bench), `HF_HUB_OFFLINE=1`, `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` | 02 §4.10; 05 §5b; bench_unify17k_m12.sh:8 | **PRESERVE** in `cluster/env` | cluster/ | — | n.a. |
| E-23 | (d) Packages unused by v12 in the env: `streaming` (MDS), `natten` [spec] | 05 §4 | **DROP** from the lock after the import-closure check | env/ | — | n.a. |

## 8. Corrections to the reports and to DESIGN.md (verified in the old repo, read-only)

| # | claim | where | verified status |
|---|---|---|---|
| R-1 | "sdpa for eval today" | DESIGN §8; 05 §4 [spec] | Wrong for every v12 entry point: they set `ATTN_BACKEND=flash_attn` (FA2) + `FUSED_MODULATE=1` (I-14). sdpa applies only when `_paths` is imported with nothing set. |
| R-2 | Timesteps "rounded to bf16" | DESIGN §5 | True for the towers (S1/S2/cont). S3 trains with fp32 t and x_t because its SLAT targets stay fp32 (T-08). Also closes 03 §2.3 / §7.9. |
| R-3 | Defect #2 (t identical on every rank) stated generally | DESIGN §6 #2; 02 §0.1 | Applies to S1, S2, SS-cont and SS-300k only. S3 draws every timestep on CUDA, per rank (T-01). |
| R-4 | Defect #10 is one item | DESIGN §6 #10 | Two separate issues: the SS tower has no voxel cap (D-07, S1/S2), and the S3 balancer starves its tail (D-08). |
| R-5 | "runners already patched 09-24" | DESIGN §6 #1 | Only the lc copies (`/fsx/data/weikai.huang/lc/{run_unify_bench,unify_render}.py`, 07:45). The in-repo `scripts/v12` copies still run the loop path, and all published unify17k numbers are loop-path (U-01). |
| R-6 | "dino_view_embed is rebuilt" | DESIGN §7 | It must be rebuilt **and bf16-rounded**, or loaded; the fp32 values differ by up to 1.65e-3 (C-23). |
| R-7 | SS-cont "unsafe to resume / will crash" | 02 §0.3 | The running 300k chain passes `EMA_INIT`/`EMA_N` explicitly and seeds the mixture with `EMA_N`. Only a bare relaunch without `EMA_INIT` hits the deleted checkpoint-25000 (T-03). |
| R-8 | `lc/pristine_repo` | 03 §0.7 ("a copy of the older demo"); 05 §3b ("a symlink farm … not a copy", "delete, or leave") | Hybrid: the top level (incl. `.git`, `scripts/`) is symlinked to the live repo, but `trellis2_blip3o/` is a real 2026-09-11 copy whose `demo_pipeline.py` differs from HEAD. Deleting it loses the code behind the sep106k image results (E-12). |
| R-9 | `prep_i1` returns `input_ids (1054,)` | 04 §1.2 | Stale docstring (`live_cond_batch.py:185`). The stock processor gives 1050 (01 measured it). |
| R-10 | SS-cont launcher = `scripts/v12/v12_ss_cont.sh` | 04 §0 table | The one that ran is `/fsx/data/weikai.huang/lc/v12_ss_cont.sh`; the repo copy is older (02 §3, 05 §2). |
| R-11 | `--vlm_model` hardcoded at `train_native_split.sh:139` | 01 §5 | It is line 138 (05 is right). |
| R-12 | eval_suite freeze = commit `text/` and `run_v12_*` | 05 §3c, §7; DESIGN §10 step 0 | Also needed: the 2026-09-02 UV-convention fix in `scripts/prepare_trellis2_e2e.py` is itself uncommitted (E-13). |
| R-13 | `load_run_connectors` breaks on S3 [unverified by execution] | 01 §6.4 | Confirmed from the config (`cond_seg_embed=False`, `cond_patch_pos='off'`) and the safetensors header (all three connectors hold BF16 stamp buffers) (U-21). |
| R-14 | `run_v12_image.py` broken [unverified] | 03 §0.7 | Confirmed: `_cond` dereferences `pack.rec` with `pack=None` (DP:101); the `build_stage_cond` monkeypatch is dead code since d8d76dd (I-22). |
| R-15 | "inference null cond must match training" [spec] | 02 §4.3, §7.4 | Resolved: `eval_cond.cond_uncond` forces `ext_drops` and reproduces the structure (C-27). |
| R-16 | Not in any report | — | HF `Trainer._prepare_input` bf16-casts dense batch tensors (T-09). In the joint-loop uncond branch, tex reads geo K/V computed from `uncond_s`, whereas in training it read them from the positive geo cond (U-05). Fixing U-04 breaks the single-cache `cache_ss_kv` assumption. `ensure_view_codes` extension scale is recovered from a bf16 norm (C-24). Import-time env mutation in `export_glb_fullchain.py:11` (I-25). |

## 9. Decisions needed (user)

Each has a recommendation. "compat" means the v12 behaviour stays reachable through `compat_v12=True`.

1. **DECIDE-1 · per-rank timesteps (T-01).** Options: (a) keep CPU-synced t; (b) draw t on CUDA per rank by default, with compat for the old draw. **Rec (b)** for BLIP3D. For the running SS-300k: leave it alone. It is a live run and the user's call, and changing t mid-run makes the continuation a different recipe. S3/unify is unaffected.
2. **DECIDE-2 · training framework (DESIGN §11.2).** HF Trainer + DeepSpeed (**rec**: easiest parity) or a custom loop. If custom, three hidden HF behaviours must be re-implemented explicitly: the bf16 cast of dense batch tensors (T-09), the accelerate shard patch / `dispatch_batches=False` (D-04), and EMA callback ordering vs checkpoint load (T-02).
3. **DECIDE-3 · S3 uncond structure (U-05).** Options: (a) keep the v12 inference (geo uses `uncond_s` with shape CFG 7.5; tex-uncond reads uncond-geo K/V); (b) match S3 training: tex-uncond reads geo K/V from the positive geo cond, and geo CFG relies on its S2-learned uncond. That costs an extra geo forward per step. (c) For any further unify training, set `joint_cond_drop=True` so geo learns an uncond. **Rec:** compat default (a) for re-reporting v12; A/B (b) on sfv50 before making it the default; (c) yes for the next unify run.
4. **DECIDE-4 · unify numbers of record (U-01).** The published unify17k numbers are loop-path, which is not the trained model. **Rec:** re-run sfv50 image + text with the fused path and publish those as "BLIP3D (v12) unified". Keep the loop numbers as history.
5. **DECIDE-5 · SS-tower voxel cap (D-07).** v12's SS tower trained on >8192-voxel assets (1.2 %); DESIGN #10 would apply one rule at manifest level. **Rec:** make the cap a per-target field. SS stays uncapped in compat and in new runs, since there is no memory reason to cap the dense SS. Apply 8192 to shape/tex/unify at load.
6. **DECIDE-6 · caption fallback policies (C-08).** Keep the two divergent v12 policies (real → long, synth → renormalise) or unify them. **Rec:** keep both as explicit fields (they are what v12 learned) and revisit only with a retrain.
7. **DECIDE-7 · I1 view law (D-18).** v12 draws I1 views uniformly over 0-15, including the 3 below-ground views, while IM down-weights them to 0.15. **Rec:** keep for compat. For new runs, consider the IM weights. Low priority.
8. **DECIDE-8 · connector precision at eval (C-17).** v12 evals ran connectors in fp32; training ran them in bf16. **Rec:** keep fp32 (v12) as the preset default and measure bf16 once on 5 cases.
9. **DECIDE-9 · tower timestep precision at inference (T-08).** Towers trained on a bf16 t grid; inference feeds fp32 t. **Rec:** keep fp32 and measure once. Switch only if the difference is visible.
10. **DECIDE-10 · numpy lock (E-05).** v12 actually ran on numpy 2.2.6 from `~/.local`. **Rec:** install 2.2.6 into the env, set `PYTHONNOUSERSITE=1`, record the goldens under that lock, and check that spconv / o_voxel / cumesh import cleanly.
11. **DECIDE-11 · TRELLIS.2 as a pinned submodule of the pushed fork (E-01; DESIGN §11.4).** **Rec:** submodule.
12. **DECIDE-12 · manifests outside the repo behind a versioned registry (D-22; DESIGN §11.5).** **Rec:** yes.
13. **DECIDE-13 · `s2_ss` history (T-05; DESIGN §11.6).** **Rec:** record it as the two-leg run it was (0-75k 2 nodes, then 75k-106k 4-node weights-only restart with EMA / optimizer / LR reset), with global step names. The recipe documents the clean intent.
14. **DECIDE-14 · per-item RNG seeding (D-03).** **Rec:** seed by default (reproducible resumes); compat = unseeded.
15. **DECIDE-15 · EMA source (T-17).** v12 EMA tracks the bf16 module weights, not the fp32 master. **Rec:** keep for parity. Consider fp32-master EMA only in a new recipe version.
16. **DECIDE-16 · keep the `interleave` mode (U-08) and the loop-path test oracle (U-01).** **Rec:** keep interleave (DESIGN §1 lists it) with the lag grid fixed (U-06). Keep the loop path only inside a test.
17. **DECIDE-17 · data provenance before any BLIP3D number (D-23, D-24).** Verify `texfixed_ff ⊆ evalclean` by sha, and geometry-hash dedupe the eval sets against the training pool. **Rec:** do it once, on a CPU node (not the login node), before recording G5.
18. **DECIDE-18 · unified model as first-class, with truly joint inference (DESIGN §11.3).** **Rec:** yes. U-02 to U-06 are the concrete fixes that make "joint" match training.
19. **DECIDE-19 · flat-colour factor convention (I-20; memory).** Still pending from 2026-09-06; it affects texture GT and metrics, not the model.

## 10. Things to do in the old repo before freezing (tag `v12-final`)

All are read-only for the running job except where noted, and none should happen before the SS-300k run finishes (its failover relaunches import the working tree). Node / hold actions stay the user's call.

1. Wait for SS-300k to finish (or for the user to stop it). Then commit the dirty working tree: `train_native.py` (EMA init), `trellis2_blip3o/geotex_sampler.py` (`JOINT_OPTS`), `trellis2_blip3o/unified_geotex.py` (`precompute_ss_kv`), `scripts/v12/monitor_sscont.sh` (E-09, U-23).
2. Copy the newer lc versions into `scripts/v12/` (`run_unify_bench.py` with `--attn fused`, `unify_render.py`, `hold_reservation.sh`, `launch_ss_cont.sh`, `v12_ss_cont.sh`, `monitor_sscont.sh`, `sscont_kill.sh`), plus the lc-only files (`ss_to300k.sh`, `ss300k_failover.sh`, `cleanup_v12.py`, `timing_*`, `demo_smoke_v12.py`, `u17_*`, `configs/mix_v12_unify_texfixed.yaml`, `configs/text_sfv_v12_eval50.jsonl`) (E-11).
3. Snapshot the out-of-repo sources into `scripts/v12/`: `_lc/v12_s1*.sh`, `launch_s1b.sh`, `v12_s2*.sh`, `launch_s2.sh`, `fork_guard*.sh`, `ss4n_ckpt_mapper.sh`, `mk_ema_init.py`, `texswap/mk_texfixed_manifests.py`, `v12_tower_probe.py`, `v12_t4_probe*.py` (E-10, T-18, D-23).
4. Force-add `tests/*` and `scripts/_tmp/{eval_v10_tasks,_hf,build_voxel_table}.py` + `scripts/_tmp/test_*.py` (E-10, E-19).
5. Archive `lc/pristine_repo/trellis2_blip3o/` (the real 2026-09-11 copy) as a tarball next to the tag, or commit it on a branch `v12-sep106k-demo` (E-12, I-22).
6. Remove or require the stale `--ema_init_from …/checkpoint-25000` default in `v12_ss_cont.sh` (T-03). This is a launcher edit, so it waits for the run to end and needs the user's OK.
7. TRELLIS.2 fork: decide on the dirty `data_toolkit` edits and untracked `data_toolkit/datasets/` (commit or stash), push `2ed033d` to `origin/blip3o-next`, and tag `v12-final` (E-01).
8. eval_suite: commit the UV fix in `scripts/prepare_trellis2_e2e.py`, `text/`, `scripts/*v12*`, and the v12 configs, then tag `v12-final` (E-13).
9. Push / tag the sibling branches `s3-edit`, `s3-rl` and the `unionpre` HEAD (E-20).
10. Record the environment facts BLIP3D must pin: `pip freeze` with and without `PYTHONNOUSERSITE=1`; the Qwen3-VL `config._attn_implementation` actually resolved by `TrainCondEncoder` (C-41); the processor config JSON (C-11); the Pillow version (C-14) (E-05, E-06, E-07).
11. Record the goldens of DESIGN §9 with the committed tree, adding these specific cases:
    - G1: train path vs eval path for i1 / im-3 / im-4 / t, including the uncond (C-05, C-27).
    - G2: unified velocities for **both** fused and loop (U-01), and the cached-geo path vs the full forward (U-03).
    - G3: latents for refine / joint / interleave with `cache_ss_kv` on and off (U-23); cascade SS through both the official sampler and the hand loop (I-11).
    - G4: batch loss per tower under the cascade assembler, plus one S3 batch; one batch dtype dump (T-08, T-09).
    - G5: 5 sfv50 cases per mode, also with the FA2 backend recorded (I-14).
12. Cheap read-only measurements: tally `truncated` in the existing unify `status.json` files (I-07); count assets >8192 voxels per pool from `voxels_v11.npz` (D-07).
13. Only after the user approves: delete `lc/patched/`, `.compile_cache/` and the in-repo stale `third_party_3d_gen/TRELLIS.2` (E-03). Never delete `pristine_repo` before step 5.
